# ICC Backend Phase 1 — Build Plan & Checkpoint Log

**Goal.** Flesh out the operational back end: invoicing, a jobs status dashboard, expenditure + a light operational P&L, and a Mark-only operator assistant. Build the first beta versions locally (no deploy), dormant behind credential flags where they touch external services.

**Provenance.** Ben's steer, 2026-09-10: "flesh out the back end, invoice generation, job logger, profit and loss tracking, expenditure, the back-end chat bot that assists Mark only". Grounded in two Opus survey passes (decisions/design + code/schema) plus primary-source verification of the load-bearing facts. Follows the D-027 `docs/*_PLAN.md` + checkpoint-log precedent.

**Deploy posture.** Everything here lands LOCAL/UNPUSHED on top of the held 2026-09-09 batch. No push, no deploy, per Ben. Schema migrations are validated on the local Supabase Docker stack only; Ben applies them to the hosted DB himself (the auto-mode classifier blocks agent schema writes). See [MEMORY / icc-supabase-db-access].

---

## Current state (verified)

- **`invoices` table already exists but is entirely unused** (`supabase/migrations/20260605115456_init.sql:140-154`): `id, job_id→jobs(restrict), status (invoice_status default draft), amount_ex_vat numeric(10,2) >0, issued_at, due_at, paid_at, created_at, updated_at`. No number, line items, provider pointer, or payment URL. `messages.invoice_id` FK + the `invoice_chase` message kind already anticipate invoice comms. Zero references in `server/`.
- **Enums** (`init.sql:30-34`): `invoice_status = draft|sent|paid|overdue`; `job_status = enquiry|booked|in_progress|completed|cancelled` (only booked/completed/cancelled are ever written today; enquiry/in_progress await the field app).
- **Stripe rail is built and proven in Test mode for deposits** (D-004/D-036): `paymentProvider.js` (Checkout Session + constant-time webhook verify), `stripe-webhook.js` (compare-and-set mark-paid). Deposit columns live on `jobs` (`deposit_status/deposit_paid_at/stripe_checkout_session_id/stripe_payment_intent_id`, migration `20260906120000`).
- **A branded A4 PDF generator exists** (`generateJobCardPDF` via `pdfkit`, in `chat.js`) — the precedent for an invoice PDF.
- **Admin surface** (`admin.html`, served at `/admin`, `requireAdmin`-gated via Supabase Auth JWT + `ADMIN_EMAILS` allowlist): job cards with per-card status, mark-complete (D-025), provisional accept/decline (D-027), a handoffs queue with AI draft/send. Filters are **date-based only** (all/upcoming/today); no status axis. The "Est. Revenue" stat regex-parses the price display string client-side, ignoring completion status.
- **Versioned API is one endpoint** (`POST /api/v1/quote`; `shared/contract/`). Every admin op (`bookings.js`, `handoffs.js`, `bookingAdmin.js`, `reviewRequest.js`) is on non-versioned `/api/*`. The contract instructs: new state/money endpoints go under `/api/v1/*`, and "factor the origin-allowlist + `rateLimit` helpers out of `chat.js` into a shared server lib" when that happens (`shared/contract/README.md:42-44`).
- **AI reuse base for the operator assistant:** `runAssistantTurn` in `chat.js` is a reusable agentic loop (tool_use/pause_turn, round-bounded, exported + tested); `handoffs.js` `draftReplyForHandoff` is the existing **operator-facing, input-minimised** AI precedent (only the captured question is sent, never PII; draft and send are separate actions; `damage_risk`/`customer_request` never auto-drafted). Model source is `shared/config/models.js` (`text = claude-sonnet-4-6`).
- **RLS** is enabled with **zero policies** on every table (`init.sql:185-194`); the server uses the service-role key (bypasses RLS). **Any new table must `enable row level security` in the same migration** or the locked-by-default posture silently breaks.
- **Migration numbering:** `YYYYMMDDHHMMSS_desc.sql`, applied lexically. Highest existing is `20260906120000`; the next is `> ` that (today 2026-09-10, so `20260910HHMMSS_...`). Each carries an inline post-apply catalog verification block + a pgTAP test in `supabase/tests/`.
- **Test pattern:** `node --test`, each file builds its own inline fakes (chainable fake Supabase, injected `now`/send fns). Functions export a pure `handlePost(event, headers, deps)`. `admin-html-syntax.test.js` guards `admin.html` edits.

---

## Two decisions to settle (recommended resolutions; Ben to confirm/override)

### Decision A — deposit-vs-invoice reconciliation
The customer pays a 10% deposit at booking. When the completion invoice is raised, it must not double-charge.

**Recommendation:** the invoice records the **full job value** (so revenue = the full service value, correct for P&L), with the **paid deposit shown as a credit line**, leaving **balance due = full − deposit**. `invoices.amount_ex_vat` stores the full total; the Stripe draft carries the job line items plus a negative "Deposit already paid" line so the hosted amount-due is the balance. ROADMAP parks *full* deposit/balance reconciliation in Phase 3; the beta implements the credit so nobody is double-charged and refines later. Dormant behind `STRIPE_SECRET_KEY`, so no live money rides on this yet.

### Decision B — P&L / expenditure crosses D-026's boundary (propose **D-039**)
D-026 (`DECISIONS.md:334-336`) deliberately split "invoicing = platform owns" from "accounting/MTD = Mark's/his accountant's duty, platform only feeds it via a generic export". Expenditure and P&L sit on the accounting side of that line.

**RATIFIED as D-039 (Ben, 2026-09-11).** Build a **light operational P&L** in-platform — revenue the platform already owns (invoices/jobs) minus a simple expense log Mark keeps — for his own at-a-glance visibility. This is explicitly **operational management insight, not formal tax accounting**: it does not do MTD digital records, quarterly ITSA, bank reconciliation, or filing, and the generic accounting export (D-026 build note 6) still feeds whatever MTD tool Mark lands on. It narrowly amends D-026 to allow cost data in-platform for operational visibility while formal tax accounting stays external. See `DECISIONS.md` D-039.

---

## Cross-cutting constraints

1. **Field app is a first-class client (D-003/D-012/D-014):** business logic server-side; new operational endpoints (invoices, jobs, expenses, P&L) go under `/api/v1/*` with a matching `.ts` contract interface, so the field app inherits them instead of forcing a retrofit.
2. **Server-authoritative figures (single source of truth):** invoice line items and the revenue side of P&L derive from `shared/config/pricing.js` `quote()` + the job row, never a client figure or the assistant's free text. No VAT (Mark not registered; `_ex_vat` names are legacy, net = gross).
3. **RLS on every new table** (enabled, no policies; service-role writes). pgTAP test + inline post-apply verification for every migration (db-migration-verification).
4. **Dormant behind a credentials flag** (the house pattern): each external-service feature reports "not configured" and changes nothing when its key is unset.
5. **Spend/auth guards:** admin endpoints gate on `requireAdmin` (fails closed). The operator assistant (LLM = spend) additionally gets the origin-allowlist + per-IP rate limit via the shared-guards refactor, plus a capped round/`max_uses` bound (guard-the-spend-paths, lock-at-the-chokepoint).
6. **AI-surface discipline** for the operator assistant: descriptive not prescriptive, read-only tools, input minimisation, human-gated for anything outbound, spend fails closed.
7. **No deploy.** Local commits on the held batch; the non-`[skip ci]` tip rule from the 2026-09-10 handover still applies at the eventual push.

---

## Build order & checklist

Ordered by what is decided and what unblocks the rest. Each beta is its own small commit(s) with tests, staged with a checkpoint-log entry below.

### Beta 1 — Jobs status dashboard  (safe, no external dep) — [x]
- **Files:** `admin.html` (add a status filter axis: outstanding vs completed vs cancelled, alongside the existing date filters; fix `updateStats` to derive revenue from job data and respect status; status badges on cards). Optionally `server/netlify/functions/bookings.js` (accept a `status` query param for server-side filtering; the field app will want this).
- **Tests:** `admin-html-syntax.test.js` (guards the edit); `bookings.js` tests if the endpoint changes.
- **Out of scope:** job-photo storage; the dead `enquiry`/`in_progress` statuses (field app); a full `/api/v1/jobs` endpoint (note as follow-up).

### Beta 2 — Invoicing (D-026)  (the anchor; dormant behind `STRIPE_SECRET_KEY`) — [x] (2a-2e done; invoicing operator-usable; accounting export built)
- **Migration** `20260910HHMMSS_invoices_provider.sql`: extend `invoices` with `provider text not null default 'stripe'`, `provider_invoice_id text`, `number text`, `payment_url text`; extend `invoice_status` with `void`, `uncollectible`; keep RLS (already enabled). Inline post-apply verification + pgTAP test.
- **Adapter** `server/netlify/functions/invoiceProvider.js`: `createDraftInvoice()`, `sendInvoice()`, `getInvoiceStatus()`; Stripe first; `INVOICE_PROVIDER` selects (default `stripe`); keys server-side only; `isInvoicingConfigured()` gate.
- **Endpoint** `server/netlify/functions/invoices.js` at `/api/v1/invoices` (netlify.toml redirect): `requireAdmin`; create-draft (find-or-create Stripe customer by email, line items from `pricing.js` + job, deposit credit per Decision A, insert local `invoices` row), review/adjust amount, send, status-reflect. Idempotent (skip if `provider_invoice_id` set); fail-closed.
- **Webhook** extend `stripe-webhook.js`: map `invoice.finalized|paid|payment_failed|voided` → `invoice_status` (constant-time signature verify; compare-and-set).
- **Admin UI** `admin.html`: an invoice panel per completed job (create/review/adjust/send/status).
- **Contract** `shared/contract/invoice.ts` + `index.ts` re-export.
- **Accounting export:** minimal invoices+payments CSV/JSON (generic, no tool coupling).
- **Tests:** `invoice-provider.test.js`, `invoices.test.js`, extend `stripe-webhook.test.js`; `admin-html-syntax` for the UI.
- **Note:** shared-guards refactor deferred to Beta 4; admin gate + idempotency + fail-closed is the defence here.

### Beta 3 — Expenditure + light operational P&L  (D-039 ratified 2026-09-11) — [x] (schema validated; expenses CRUD + cash-basis P&L; finances admin UI)
- **Decision:** D-039 accepted (see `DECISIONS.md`); build.
- **Migration** `expenses` table: `id, created_at, updated_at, incurred_on date, category (enum: fuel|materials|equipment|insurance|software|other), description, amount numeric(10,2) >0, job_id uuid null references jobs(id) on delete set null, notes`. RLS enabled. pgTAP + verification.
- **Endpoint** `/api/v1/expenses` (`requireAdmin`, CRUD) + `/api/v1/pnl` (period revenue from invoices/jobs − expenses).
- **Admin UI:** expense entry + a P&L summary panel.
- **Contract** types.
- **Tests:** endpoint + pure aggregation.

### Beta 4 — Operator assistant (Mark-only)  (last; D-040 accepted 2026-09-11) — [x] (BUILT 2026-09-13 to the D-040 addendum contract + build notes; the bullets below are the original sketch, superseded by DECISIONS.md D-040 and the checkpoint entries)
- **Decision:** D-040 accepted (read-only, input-minimised, descriptive, admin-gated, spend-capped; see `DECISIONS.md`); build.
- **Prerequisite refactor:** extract the origin-allowlist + per-IP `rateLimit` from `chat.js` into a shared server lib (`shared/contract/README.md:42-44`); behaviour-preserving, locked by `hardening.test.js`/`origins.test.js`.
- **Endpoint** `/api/v1/operator-chat` (`requireAdmin` + shared guards + capped rounds/`max_uses`): reuses `runAssistantTurn`; a distinct operator system prompt; **read-only** tools (query jobs / outstanding invoices / revenue / expenses / P&L); descriptive output; never sends anything outbound itself.
- **Admin UI:** an operator-assistant panel.
- **Contract** types.
- **Tests:** loop reuse, tool routing, gate/spend-cap, fail-closed.

---

## Out of scope (phase 1)
Job-photo storage (bucket not stood up); field-app UI; live Stripe money (go-live gate: domain cutover + noindex removal); formal MTD accounting/filing (stays external, D-026); PECR-gated marketing (Phase 4); full deposit/balance reconciliation beyond crediting the paid deposit (Phase 3); calendar clash-check (parked, D-033).

---

## Checkpoint log (append one entry per commit, staged with it)

_Unit opened 2026-09-10._

- **Beta 1 — jobs status dashboard** (this commit). `admin.html` only. Added Outstanding / Completed / Cancelled status filters keyed off the real `jobs.status` (new pure `jobStatusCategory(b)` helper), a `.status-cancelled` badge, status-accurate card labels for booked / in_progress / cancelled, and excluded cancelled jobs from the Upcoming / This Week / Revenue stats. Legacy Blobs rows (no `job_status`) match only the date filters, by design. **Deviation:** kept the existing revenue display-string parse (a numeric revenue field is deferred to the P&L slice, `TODO(backend-phase1/pnl)` planted in `admin.html`); did not add a `/api/v1/jobs` endpoint (follow-up for the field app). **Verify:** `node --test` green incl. the `admin-html-syntax` guard; full dashboard behaviour is login-gated, so Ben confirms live.
- **Beta 2a — invoices schema** (this commit; D-026 build note 1). `supabase/migrations/20260910120000_invoices_provider.sql` extends the unused `invoices` table with `provider`/`provider_invoice_id`/`number`/`payment_url` + a partial-unique index on `provider_invoice_id`, and adds `void`/`uncollectible` to `invoice_status`. `supabase/tests/invoices_provider_test.sql` proves columns/defaults/enum/index and that a duplicate `provider_invoice_id` is rejected. **Validated on the local Docker stack:** `supabase db reset` applied it cleanly in sequence; `supabase test db` = 5 files / 49 tests PASS. Ben applies to hosted (auto-mode blocks agent schema writes); the inline post-apply catalog verification block is in the migration.
- **Beta 2b — invoice adapter** (this commit; D-026 build note 2). `server/netlify/functions/invoiceProvider.js`: provider-agnostic Stripe Invoicing adapter mirroring `paymentProvider.js` (raw REST, injectable fetch, fail-closed, dormant behind `STRIPE_SECRET_KEY` / `INVOICE_PROVIDER`). `findOrCreateStripeCustomer`, `createDraftInvoice` (server-side integer-pence lines, incl. a negative deposit-credit line per Decision A), `sendInvoice` (finalize + send), `getInvoiceStatus`, `mapStripeStatus` (Stripe → our enum; `open` → `sent`, overdue derived from due date not carried). `test/invoice-provider.test.js` (9 tests): routing, param-building, the negative credit line, idempotency-key, non-integer rejection, fail-closed. **Verify:** `node --test` 392 tests / 383 pass / 0 fail. No endpoint wired yet (2c).
- **Beta 2c — invoice endpoint** (this commit; D-026 build notes 3-4). `server/netlify/functions/invoices.js`: `POST /api/v1/invoices` (create/send/status) + `GET` (list), admin-gated (`requireAdmin`), dormant behind `STRIPE_SECRET_KEY`, fail-closed. **create** raises a Stripe draft against a COMPLETED job from the server-authoritative stored total (`jobs.estimated_price_ex_vat`, or Mark's reviewed `amount_ex_vat` override), with a negative deposit-credit line when the deposit is paid (Decision A); idempotent (one invoice/job). **send** finalises + emails; **status** reflects (sets `paid_at` on paid). Routed in `netlify.toml`; typed in `shared/contract/invoice.ts` (+ index barrel). `test/invoices.test.js` (12 tests). **Verify:** `node --test` 404 / 395 pass / 0 fail. **Deviation:** a single service line from the stored total (the job doesn't store itemised `quote_lines`; itemisation is a follow-up); create-then-record ordering leaves a low-harm orphan-draft window on a local-insert failure (the Stripe idempotency key dedupes retries; `TODO(backend-phase1/invoice-orphan)` planted). Admin UI (2e) + webhook (2d) still to wire.
- **Beta 2d — webhook invoice reflection** (this commit; D-026 build note 5). Extended `stripe-webhook.js` to reflect `invoice.*` events onto the `invoices` row keyed by `provider_invoice_id`: `invoice.paid` (compare-and-set on `status <> 'paid'`, stamps `paid_at` + number + url), `invoice.finalized` (`sent` + number + url), `invoice.voided` (`void`), `invoice.marked_uncollectible` (`uncollectible`); `invoice.payment_failed` ignored (overdue derives from `due_at`). Reuses the existing constant-time signature verify (the webhook route already exists). `test/stripe-webhook.test.js` +5 tests (`neq` added to the fake). **Verify:** `node --test` 409 / 400 pass / 0 fail. Closes the get-paid→track loop without polling; the endpoint `status` action stays a manual refresh. Remaining for Beta 2: the admin UI (2e) + the accounting export.
- **Beta 2 hardening** (this commit; from the Opus-5 advisor pass). `handleGet` now DERIVES `overdue` from `due_at` for `sent` invoices — the enum value and the contract type both promise `overdue` but nothing produced it, so the list would have shown a past-due invoice as `sent` (enforce-invariants-in-build). +1 test. Removed the unused `exports.reflectInvoiceEvent` (earn-every-line; the tests reach it via `handlePost`). `provider` staying hardcoded `'stripe'` is intentional (one rail today; deriving it would be speculative). **Verify:** `node --test` 410 / 401 pass / 0 fail.

_Unit resumed 2026-09-13 (Beta 2e; UI + export)._

- **Baseline fix (pre-Beta-2e, separate commit).** `test/booking-admin.test.js` `baseJob()` hardcoded `slot_date: "2026-09-10"`, which went past by 2026-09-13 and tripped the `resend` "day has passed" 409 guard, reddening two resend tests (a stale date-fixture, not a product regression — the batch's suite was RED as of today). Fixed to a relative future date (`Date.now() + 30d`), mirroring `bookings-chat.js` `futureDow`. Tests needing a past booking already override to a fixed past date. LESSONS_LEARNED candidate (date-relative fixtures) prompted to Ben.
- **Beta 2e — invoice admin UI + `configured` signal** (this commit; D-026 build notes 3-4). `admin.html`: a per-completed-job invoice panel (create draft from the server-authoritative figure with an optional amount override, review, Send = finalise + email, Refresh status; status badge + total + number + hosted-invoice link), rendered via a pure `invoicePanelHtml(b, inv, configured)` into a per-job container so create/send/refresh re-render in place (no full reload wiping open cards). `loadInvoices()` loads the list in parallel with bookings and never rejects (a failed read leaves cards usable). `invoices.js` `handleGet` now returns `{ invoices, configured }` (the dormant signal, mirroring `handlePost`) so the panel shows "not configured" instead of offering a create that would 503; `configured: boolean` added to `InvoiceListResponse`. **Deviation:** the panel can't show the deposit-credited balance (neither `/api/bookings` nor the invoice row carries `deposit_status`/amount-due), so it shows the full invoice total + a general note; surfacing the balance would need a feed/row addition (flagged to Ben). **Verify:** `node --test` 412 / 403 pass / 0 fail; `admin-html-syntax` green. UI is login-gated — Ben confirms live. Orphan flag (`invoices.js`) sharpened with the real residual harm (changed-override retry → stray pending item); the one-click Create retry is the practical mitigation, so the TODO stays deferred.
- **Beta 2e — accounting export** (this commit; D-026 build note 6, D-039, **D-041** two-receipts ratified by Ben 2026-09-13). `server/netlify/functions/accountingExport.js` at `GET /api/v1/accounting-export` (admin-gated, reads our own DB so not gated on the Stripe rail): an invoices REGISTER + a payments FEED. Payments use the two-receipts model — a paid deposit (at `deposit_paid_at`) and the invoice BALANCE (`full − deposit`, at `paid_at`) are separate receipts that sum to the full job value, no double-count, correct dates; a no-deposit paid invoice is one full receipt; unpaid invoices stay in the register only. JSON by default; `?format=csv&entity=payments|invoices` returns one CSV (RFC-4180 quoted). Typed in `shared/contract/accounting.ts` (+ index barrel); routed in `netlify.toml`; two admin CSV-download buttons (`admin.html`, fetch-with-Bearer + Blob). `test/accounting-export.test.js` (9 tests): the receipt math incl. the sum-to-full invariant, no-deposit, unpaid-excluded, deposit-without-invoice, overdue derivation, CSV escaping, json/csv routing, 503. **Deviation:** refunded deposits excluded from the first cut (`TODO(backend-phase1/accounting-refunds)`); money rendered as raw numbers (not 2dp-formatted) in CSV. **Verify:** `node --test` 421 / 412 pass / 0 fail.

_Beta 3 (expenses + operational P&L, D-039) opened 2026-09-13._

- **Beta 3 slice A — expenses schema** (this commit; D-039). `supabase/migrations/20260913120000_expenses.sql`: an `expenses` table (incurred_on date, category enum fuel|materials|equipment|insurance|software|other, description, amount numeric(10,2) > 0, job_id → jobs ON DELETE SET NULL so a job delete keeps the cost row, notes, timestamps + `set_updated_at` trigger), RLS enabled (no policies), inline post-apply catalog verification. `supabase/tests/expenses_test.sql` (pgTAP, 11): columns/nullability, enum labels, RLS on, the amount > 0 check rejects 0, and job_id SET NULL on job delete. **Validated on the local Docker stack:** `supabase db reset` applied all 10 migrations in sequence cleanly; `supabase test db` = 6 files / 60 tests PASS. Ben applies to hosted himself (auto-mode blocks agent schema writes). **migration-before-deploy:** unlike invoicing, the expenses/P&L endpoints have NO key gate, so they must ship only after the table exists on hosted; slices C/D fail clean on Postgres `42P01` (undefined_table) meanwhile.
- **Beta 3 slice B — receipts.js money-in single source** (this commit; refactor, D-041). Moved the invoices-register + payments-feed derivation (`buildInvoiceRegister`, `buildPayments`) and the two DB loaders out of `accountingExport.js` into `server/netlify/functions/receipts.js`, re-exported from `accountingExport.js` so its 9 tests exercise the shared source UNCHANGED (behaviour-preserving; the tests did not move, which is what proves it). `pnl.js` (slice D) requires the same module, so the P&L revenue and the accountant feed cannot diverge. **Verify:** `node --test` accounting-export 9/9 green.
- **Beta 3 slice C — expenses endpoint** (this commit; D-039). `server/netlify/functions/expenses.js` at `/api/v1/expenses` (admin-gated, REST verbs): POST create, GET list (optional `?from&to` on incurred_on), PATCH update, DELETE. Category validated against the enum SERVER-SIDE (400, never a DB 500); amount forced positive + 2dp; a missing table (Postgres `42P01`) → clean 503 "not set up yet" (migration-before-deploy safety), a bad job_id FK (`23503`) → 400. Shared `period.js` (from/to YYYY-MM-DD, UTC calendar days, default current month; inclusive date range, end-exclusive timestamp bound) so the expenses list and the P&L cannot disagree on a period. Typed in `shared/contract/expense.ts`; routed in `netlify.toml`. `test/expenses.test.js` (13): routing, validation, the 42P01/FK mapping, the period filter. **Verify:** `node --test` 434 / 425 pass / 0 fail.
- **Beta 3 slice D — operational P&L endpoint** (this commit; D-039, **D-042**). `server/netlify/functions/pnl.js` at `GET /api/v1/pnl?from&to` (admin-gated): revenue (cash received in the period via `receipts.buildPayments` — the SAME feed the export uses, so the margin reconciles) − expenses (cost rows with incurred_on in the period) = margin, plus revenue-by-type + expenses-by-category breakdowns. Period via the shared `period.js` (default current month, end-exclusive timestamp window). A pending migration on hosted (`42P01` undefined_table OR `42703` undefined_column — the P&L reads the invoices provider columns) → clean 503; the `receipts.js` loaders now preserve the pg error code for this, and the export's catch maps it the same way. Typed in `shared/contract/pnl.ts`; routed in `netlify.toml`. **D-042 recorded** (cash-basis P&L). `test/pnl.test.js` (7): the end-exclusive window boundary, revenue-by-type/margin, float-drift rounding, reconciliation with the export's receipts, and the 42P01→503 mapping. **Verify:** `node --test` 441 / 432 pass / 0 fail.
- **Beta 3 slice E — finances admin UI** (this commit; D-039/D-042). `admin.html`: a Finances section with a period picker (defaults to the current month), a P&L panel (Revenue cash-in / Expenses / Margin cards + revenue-by-type and expenses-by-category breakdowns, margin colour-coded), an add-expense form (date / category / amount / description) and a period expense list with delete. `loadFinances()` pulls `/api/v1/pnl` + `/api/v1/expenses` for the period together; a not-set-up 503 (migration not yet applied on hosted) or any error renders a plain message, never a blank panel. Closes the Beta 1 `TODO(backend-phase1/pnl)` flag: Est. Revenue stays the PIPELINE estimate, the cash-received Revenue lives in the P&L panel (two different numbers, on purpose). **Verify:** `node --test` 441 / 432 pass / 0 fail incl. the admin-html-syntax guard; UI is login-gated so Ben confirms live. **Beta 3 COMPLETE** (schema validated on the local stack, both endpoints unit-tested, finances UI built).
- **Beta 4 — DESIGN converged (no code yet)** (2026-09-13; cross-agent peer-design review CLAUDE + GPT/ASTRA + Gemini/GEMPRO, both `[[CONVERGED]]`, no residual). The converged build contract is recorded as the **D-040 implementation-contract addendum in DECISIONS.md** (facade + allowlist module-boundary test instead of a DB read-only role; Postgres atomic identity-keyed fail-closed spend counter; budget-aware bounded loop; inert render; aggregates-by-default). Transcript in the gitignored `exchange/REVIEW_beta4-operator-assistant_2026-09-13.md` (machine-local). Build next session in 5 slices: origins.js refactor → `operator_rate` migration + atomic admission → `readOnlyClient` facade + allowlist test + extract `buildPnl` → `/api/v1/operator-chat` → admin panel. Two brief errors caught + corrected in the addendum (`max_uses` is web-search-only; `runAssistantTurn` yields no structured results). **Phase 1 status:** Beta 1, 2 (2a-2e), 3 complete; Beta 4 designed, build pending.

_Beta 4 (operator assistant, D-040 addendum contract) opened 2026-09-13._

- **Beta 4 slice 1 — origins.js extraction** (this commit; D-040 guardrail 4, behaviour-preserving). Moved the origin allowlist + CORS logic out of `chat.js` (the module-head `normaliseOrigins` / `netlifyDeployOrigins` / `buildAllowedOrigins` block plus `getOrigin` / `corsHeaders` / the inline strict-vs-fail-open check) into `server/netlify/functions/origins.js`. `createOriginPolicy({ methods, headers, log })` snapshots the env-derived allowlist + mode once (as the old module-level consts did) and returns `corsHeaders(origin)` + `check(origin)`; `chat.js` builds one policy at load beside the rate limiter and uses it at the same two points as before (same order: OPTIONS → 405 → key → JSON → origin check). The rate limiter was already in `rateLimit.js`, so this completes the shared-guards note in `shared/contract/README.md` (updated). `test/origins.test.js` now imports from `origins.js`: the 5 existing allowlist tests unchanged + `getOrigin` parsing + the request/response MATRIX ({strict, fail-open} × {allowed, foreign, absent origin} → verdict, ACAO echo/fallback, Vary, per-endpoint header/method overrides, the diagnostic log). Mutation check: disabling the strict branch reds 3 matrix tests. **Verify:** `node --test` 452 / 443 pass / 0 fail / 9 skip. **Citation re-base:** the D-040 addendum cited the tool-handler catch as `chat.js:488-498`; the head removal shifted it to `chat.js:416-426` (DECISIONS.md updated in this commit rather than left to drift). **Deviation:** none; `exports.buildAllowedOrigins` dropped from `chat.js` (its only importer was the test, which moved). Not touched: the other endpoints' `Access-Control-Allow-Origin: *` (Bearer-gated, not origin-gated, by design).
- **Beta 4 slice 2 — `operator_rate` atomic turn budget** (this commit; D-040 addendum "spend is a Postgres atomic counter"). `supabase/migrations/20260913180000_operator_rate.sql`: `operator_rate(user_id uuid, window_start timestamptz, count int >= 0, PK(user_id, window_start))`, RLS enabled/no policies; `operator_admit(p_user_id, p_limit) returns boolean` (language sql): prune windows older than 2 days, then ONE `INSERT … ON CONFLICT DO UPDATE SET count+1 WHERE count < p_limit RETURNING true`, wrapped in `coalesce(…, false)` so the result is a DEFINITE boolean (true admitted / false over-limit-or-limit<1), never NULL. EXECUTE **revoked from public/anon/authenticated**, granted to service_role: the local catalog confirms Supabase's default ACL gives `anon` EXECUTE on every new public function (an unrevoked probe function showed `anon = true`), so without the revoke the public API key could burn Mark's budget through PostgREST. `supabase/tests/operator_rate_test.sql` (pgTAP, 19): shape, RLS, function + the privilege boundary (anon/authenticated false, service_role true), limit-2 → t,t,f with the counter parked at 2, per-user independence, limit 0 → false + no row, negative count rejected, stale-window prune. **Validated on the local Docker stack:** `db reset` applied all 12 migrations in sequence; `test db` = 7 files / 79 tests PASS; the migration's inline post-apply block run by hand (columns, RLS t, privs f/f/t, admit×2 at limit 1 → t,f). `server/netlify/functions/operatorAdmission.js`: `admitOperatorTurn(supabase, userId, limit)` → `{admitted:true}` | `{admitted:false, reason: over_limit | unavailable | ambiguous | bad_identity}`; only a literal `true` admits; a non-uuid identity never reaches the DB; `OPERATOR_TURN_LIMIT` (default 30/hour, a typo falls back to the default) + `secondsToNextWindow` for Retry-After. `test/operator-admission.test.js`: 9 unit (the mapping, identity guard, limit parsing) + the **[integration] last-slot race**: ten independent supabase-js clients at count = limit−1 → exactly one `admitted`, nine `over_limit`, counter = limit; mutation (seed two slots) → exactly two admitted and the assertion fires. **Verify:** `node --test` 462 / 452 pass / 0 fail / 10 skip (the race self-skips without `ICC_SUPABASE_IT=1`; run green here against the local stack). `.env.example` documents the var. **Ben applies to hosted** (third pending migration for the held batch, after `20260910120000` and `20260913120000`). **Deviation:** none from the addendum; D2 (one hourly window) is a plan choice, `TODO(backend-phase1/operator-daily-cap)` to be planted at the endpoint in slice 4 if a second window is wanted. Also this session: `supabase/config.toml` `[analytics] enabled = false` (separate chore commit, local-only; the Vector log shipper was crash-looping on this machine).
- **Beta 4 slice 3 — read-only facade + module-boundary control + `pnlCalc.js`** (this commit; D-040 addendum "read-only enforcement is architectural, not a DB role"). `server/netlify/functions/readOnlyClient.js`: `createReadOnlyClient(supabase, allowlist)` — the ONLY data access the operator tool handlers will get. Allowlisted tables; `select()` takes an explicit column list validated per table, plus ONE level of embed with its own per-relation column allowlist (`invoices → jobs(slot_date,deposit_ex_vat,deposit_status)`), because `receipts.buildPayments` derives an invoice's balance receipt as full − deposit from the embedded job (without it the tool's P&L would double-count deposits and diverge from the export, D-041); `*`, aliases, casts, `!inner` hints, nesting and any other relation are rejected. Chain = `eq/gte/lte/order` on allowlisted columns only, `limit` REQUIRED (1..5000; an unbounded query cannot run), `await` → a fresh `{data, error}` with the error reduced to `{code, message}`. Every other property at every stage throws via a Proxy get trap (`insert/update/delete/upsert/rpc/storage/auth/channel/url/...`); Symbol keys, `constructor`, `toJSON` read as undefined so `util.inspect`/`JSON.stringify`/`deepStrictEqual` never trip it; the real builder is closure-held, never a property. `test/read-only-client.test.js` (12): positive real chains (order + normalised projection recorded at the fake client), error reduction, immutability/branching, and negatives at EVERY stage with the fake proving the client was never touched; mutation (drop the column check) reds it. `server/netlify/functions/pnlCalc.js`: `buildPnl` + `round2` moved VERBATIM out of `pnl.js` (imports only `period.js`); `pnl.js` requires and re-exports it, so `test/pnl.test.js` (7) is UNCHANGED and green — the proof of behaviour preservation, same pattern as `receipts.js`. `test-support/moduleGraph.js`: a STATIC transitive `require("<literal>")` walker (comments stripped, relative specs resolved via `require.resolve`, bare specs recorded, a non-literal argument = violation on sight) + `checkAllowlist`. Static rather than a `Module._load` hook because a hook only sees load-time requires and would pass a `require("./supabaseClient.js")` inside a handler body. `test/module-boundary.test.js` (7): pnlCalc positive; the four client-free modules reach no privileged module; three NEGATIVE fixtures that must fail (`test-support/fixtures/`: indirect-via-helper, lazy-inside-a-function, dynamic argument) + bare-import handling + comment stripping. Support files live in `test-support/`, not `test/`, because Node's default glob loads every `.js` under `test/` and reported them as five vacuous passing files. **Verify:** `node --test` 481 / 471 pass / 0 fail / 10 skip. **Deviation:** none; the embed allowance is the D-041 correction from the advisor pass (the plan's first draft rejected all embeds). Slice 4 adds the positive assertion over the real `operatorTools.js` graph.
- **Beta 4 slice 4 — `POST /api/v1/operator-chat`** (this commit; the D-040 addendum contract, built). `server/netlify/functions/operatorTools.js`: the ALLOWLIST (jobs / invoices / expenses; `customers(name)` as an embed; invoices → jobs → customers(name) as a nested embed) and seven read-only tools — `jobs_summary`, `jobs_list`, `find_customer_jobs`, `invoices_list` (overdue derived from `due_at`, deposit credited shown), `pnl` (`receipts.buildPayments` + `pnlCalc.buildPnl`, NO customer columns requested), `expenses_summary`, `expenses_list` — every one bounded (CAPS) with an explicit `partial` flag, closed input schemas validated server-side (unknown keys such as `include_pii`/`table`/`sql`/`url` refused, so a model-invented flag cannot widen scope), handlers that never throw (unknown tool, bad args, DB error, missing table and a facade violation all return structured JSON), customer-typed strings capped + control-stripped (`safeText`). **Data scope per Ben (2026-09-13):** customer name + job postcode ARE returned (the plan's zero-PII first cut was overridden); email / phone / address line / every customer free-text field stay out, pinned by `test/operator-tools.test.js` over the allowlist. `server/netlify/functions/operatorChat.js`: gate order origin → `requireAdmin` → per-IP (`rl:opchat`, 60/h, fail-open as ever) → bounded transcript (≤ 20 messages, ≤ 4000 chars, alternating, user last) → API key + Supabase configured → `admitOperatorTurn` on the VERIFIED user id (429 + `Retry-After` to the next hour / 503 fail-closed) → one turn: `makeBudgetedCallModel` (≤ 4 model calls incl. pause_turn/post-tool; `max_tokens` 1200; deadline `OPERATOR_TURN_DEADLINE_MS` default 8500 checked before each call AND passed as `AbortSignal.timeout`; exhaustion THROWS `BudgetExhausted` out of `runAssistantTurn`), `makeDispatchGuard` (≤ 6 tool dispatches counted BEFORE each handler), `runAssistantTurn(…, 3)`; a turn that ends on the loop's own round cap with a tool still pending is reported as `stopped: "model_calls"` too, never an empty reply; `withSingleTextBlock` → `{ content:[text], usage }`; model failures → 502 with no detail. `server/netlify/functions/assistantLoop.js`: `runAssistantTurn` + `withSingleTextBlock` moved VERBATIM out of `chat.js` (which re-exports them; `test/escalation.test.js` + `test/websearch.test.js` unchanged, 31 green) so the operator function does not bundle the customer chat. `netlify.toml` route; `shared/contract/operatorChat.ts` (+ barrel); `.env.example` documents `OPERATOR_TURN_DEADLINE_MS`. **Privacy notice** (`site/src/pages/privacy.astro` AND its `index.html` mirror, both "Last updated: 13 September 2026"): a lawful-basis bullet, an "Our AI assistant" paragraph naming the operator use, the fields sent (name, postcode, date, status, amounts), the exclusions, read-only, and the no-training term; the Anthropic sharing bullet extended. Tests: `test/operator-tools.test.js` (13, through the REAL facade over `test-support/fakeReadStore.js`, which applies projections so leaked columns would show; exact key sets; hostile-name-as-data; two mutations caught: an unallowlisted projection reds 4, a widened allowlist reds the notice-invariant test), `test/operator-chat.test.js` (18: gate order with "nothing else ran" proofs, admission keyed on the verified id not a body field, 429/503 mapping, the request the model sees, a tool round, end-to-end through the real tools, budget exhaustion terminal with the customer prose asserted ABSENT, the production-limits round-cap stop, clock-jump + fetch-timeout deadlines, the dispatch guard, 502 without detail, a hostile tool result travelling as data with only the model URL ever fetched, strict-origin 403 via a fresh module load), `test/module-boundary.test.js` +2 (**THE BOUNDARY**: `operatorTools.js` reaches exactly {operatorTools, pnlCalc, period, receipts}, no bare imports, no dynamic requires — a lazy `require("./supabaseClient.js")` appended to it reds the test; and `operatorChat.js` reaches auth/client/admission but not `chat.js` or any provider). **Verify:** `node --test` 515 / 505 pass / 0 fail / 10 skip. **Deviations from the plan (recorded in the D-040 build notes):** PII scope widened (Ben); embeds recursive-allowlisted rather than "one level"; the loop extracted to `assistantLoop.js`. **Still to do:** slice 5 (admin panel), Ben's Netlify function-timeout check (the 8.5 s default assumes the 10 s ceiling), the with-Ben real ride after deploy.
- **Beta 4 slice 4 hardening** (this commit; from the second advisor pass, all eight points taken). Deadline anchored at function ENTRY (`startMs` before requireAdmin / limiter / admission, so gate latency counts against the 8.5 s, not on top of it; tested with 3 s + 3 s gate delays); `invoices_list` gains `balance_due_ex_vat` per row (face value less the PAID deposit for draft/sent/overdue, 0 once paid/void/uncollectible) + `total_balance_due_ex_vat`, and prompt rule 4 says which figure answers "outstanding" (fixture: INV-0004, 250 with a paid £24 deposit → 226 due; overdue face total 500 vs 476 actually collectable); `pnl.partial` is a boolean (the flag rule 3 promises) with `partial_detail`; `max_tokens` 1,600 and a `stop_reason: max_tokens` reply returns `truncated: true`; the operator per-IP 429 no longer tells Mark to phone his own number; the D-040 citation re-based to `assistantLoop.js:37-47`; spend priced into the D-040 build notes (≈ £0.40 worst-case turn, ≈ £0.02 typical, ≈ £24/hour adversarial ceiling at 30/h × 2 operators). NEW `[integration]` test: all seven projections resolve on a real PostgREST (a non-existent allowlisted column reds it via 42703). **Verify:** `node --test` 518 / 507 pass / 0 fail / 11 skip; the two `[integration]` tests green against the local stack.
- **Beta 4 slice 5 — admin operator panel** (this commit; D-040 addendum "operator reply rendered inert"). `admin.html`: an "Ask about the business" section under Finances — a hint line (read-only, describes never acts, no tax/legal advice, ex VAT, revenue = cash received), a transcript, a textarea (`maxlength` 4000, Enter sends, Shift+Enter newline), Ask + Clear. Render path is `createElement` + `textContent` ONLY (no innerHTML / markdown / links), so a reply quoting customer-typed text is inert: verified in the browser with `<img src=x onerror=alert(1)>` in a message → rendered as literal text, 0 `img` elements. Client-held transcript bounded to `OPERATOR_MAX_HISTORY` = 20 (asserted equal to the server's `LIMITS.maxHistory`). A 429 / 503 / 400 / 502 / `stopped` outcome NEVER enters the transcript as an assistant turn: the question goes back into the box with a plain status line, so the replayed history keeps alternating and the server's 400 check cannot trip (verified with a stubbed `fetch`: normal → appended; 429 → boxed + error line; stopped → boxed; `truncated` → appended + "cut short" line; every request sent was user-first/user-last). Send disabled while in flight; a fresh transcript per login, cleared on logout. `test/admin-html-syntax.test.js` +1: the operator block (comments stripped) contains no `innerHTML` / `outerHTML` / `insertAdjacentHTML` / `document.write` / `eval` / `new Function`, renders via `textContent`, pushes an assistant reply only inside the ok-and-not-stopped branch, and mirrors the server cap; two mutations (innerHTML in the render path; replaying a stopped reply) red it. **Verify:** `node --test` 519 / 508 pass / 0 fail / 11 skip. Login-gated, so the real ride (a live model turn over hosted data) is a with-Ben step after the batch deploys. **Beta 4 COMPLETE** (five slices + hardening); phase 1 build closed pending deploy + rides.
