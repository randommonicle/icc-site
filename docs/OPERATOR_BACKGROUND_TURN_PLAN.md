# Operator assistant background turn — Build Plan & Checkpoint Log

**Goal.** Move the D-040 operator assistant's turn off the 10 s synchronous-function ceiling (L-042: the first turn after every deploy, and any two-tool question, died on the 8.5 s deadline) by running it in a Netlify background function, with the admin page polling for the result. Stability over cleverness (Ben, 17 Sept 2026: "option three, it seems the most stable").

**Provenance.** L-042 (first live ride, 16 Sept 2026) and ROADMAP "Operator assistant turn headroom". Options weighed 17 Sept: pre-warm on login (fixes only the cold turn), trim the turn (fixes only the warm two-tool case; prompt caching was already in place at `operatorChat.js:220`), or a background function (removes the ceiling). Gate checked before planning: the Netlify account is `type_slug: credit-personal` and the API reports `background_functions: { included: true }` (1,000 credits a month, 0 used this period); the docs list background functions on the Free, Personal and Pro credit plans, 15 minutes per invocation, a 202 on invocation. Follows the `docs/*_PLAN.md` + checkpoint-log precedent.

**Deploy posture.** Built on `claude/operator-background-turn`, each commit `node --test` green, pushed to `main` as ONE batch only after the `operator_turns` migration is applied to hosted Supabase by Ben and catalog-verified (D-043 pre-push hook enforces this; push to main deploys). Then one real ride: a two-tool question asked cold, straight after the deploy.

**Shape.** `POST /api/v1/operator-chat` keeps every gate (origin, requireAdmin, per-IP, body, config, atomic admission) and, instead of running the turn, inserts an `operator_turns` row (`queued`, the verified user id, the validated transcript), triggers `operatorTurn-background` over HTTPS with `{ turn_id }`, and answers 202 `{ turn_id }`. The background function claims the row by compare-and-set (`queued -> running`, filtered on id AND status; zero rows claimed means nothing runs, which is the named primary spend defence for a public function URL and makes any platform retry a no-op), runs the turn with the deadline anchored at its own entry (default now 60 s), and records `done` / `stopped` / `failed` only where `status = 'running'`. `GET /api/v1/operator-chat?turn=<uuid>` (requireAdmin, own rows only, 404 otherwise) returns the state; the panel polls (first at 3 s, then every 2 s, cap 90 s). Rows older than an hour are pruned on each start call. D-045 records the contract change from D-040's "stateless with bounded replay".

**Spend.** No new recurring job. Per turn: one background invocation for the length of the turn plus two to five cheap polls (Netlify: 2 credits per 10k requests, 10 credits per GB-hour), roughly 0.04 credits; about 25 credits a month at 20 questions a day. Anthropic spend per completed turn unchanged (D-040 pricing stands).

---

## Checklist

- [x] 1. Runner extraction (no behaviour change): `operatorTurnRunner.js` holds the turn; `operatorChat.js` calls it; the existing suite untouched and green; the runner's own contract tests added.
- [x] 2. Migration `operator_turns` (RLS enabled, no policies, service role only) with catalog verification queries in the file; local `db reset` green.
- [x] 3. `operatorTurnStore.js` (the only module that knows the table: enqueue / claim / finish / read / prune) + `operatorTurn-background.js` (claim, run, record; a throw lands as `failed`). Unit tests with a fake store; `[integration]` claim race (exactly one of N claims wins; both win when the status filter is dropped, the proof it can fail).
- [x] 4. `operatorChat.js`: enqueue + trigger + 202; `GET ?turn=` poll; header and deadline comments rewritten; the Cheltenham word in the operator prompt fixed; `netlify.toml` CORS gains GET; `.env.example` default. Tests: the gate cases unchanged, the trigger is the only fetch, trigger failure marks the row failed and answers 503, the poll shapes and scoping.
- [x] 5. `admin.html`: the polling loop, one timeout message, the wording of the waiting line; `admin-html-syntax` pins survive and gain "client cap exceeds the server deadline".
- [x] 6. Docs: D-045, L-042 addendum, ROADMAP ticks (and the stale "first invoicing ride still owed" at line 70), `docs/STRIPE_SETUP.md:53` events list once Ben has added them, NEXT_SESSION entry; close this log with the checklist walk.
- [ ] 7. Migration applied to hosted by Ben, catalog-verified; push; deploy `ready`; the cold two-tool ride; background function log shows the turn line and the trigger answered 202.

---

## Checkpoint entries (newest last)

- **1. Runner extraction** (this commit; no behaviour change). `server/netlify/functions/operatorTurnRunner.js` now holds LIMITS, deadlineMs, the static prompt (byte-identical, checked), STOPPED_TEXT, BudgetExhausted, the budgeted callModel, the dispatch guard, todayLine and `runOperatorTurn(messages, deps)`, which returns `{ kind: done | stopped | failed, ... }` instead of an HTTP response; the caller supplies `deadlineAt` (absolute), so the anchor stays the caller's decision. `operatorChat.js` keeps the six gates and maps the result to the same 200 / 200-stopped / 502 bodies as before, re-exporting the runner's pieces so `test/operator-chat.test.js` and `admin-html-syntax.test.js` run untouched (557 tests, 546 pass, 0 fail, 11 skip, after rebuilding `site/dist`, which was a 10 Sept build and reddened the two `[build]` guards on its own). `test/operator-turn-runner.test.js` pins the same turn contract against the result object; the duplicated turn tests leave `operator-chat.test.js` in step 4 when the endpoint stops running the turn.
- **2. Migration `operator_turns`** (this commit). `supabase/migrations/20260917220000_operator_turns.sql`: `id uuid pk default gen_random_uuid()`, `user_id`, `status` check-constrained to the five states, `messages jsonb` check-constrained to a JSON array, `result jsonb`, `created_at default now()`, `started_at`, `finished_at`; RLS enabled, no policies, no functions (the claim and the record are plain filtered updates through PostgREST, so there is nothing to revoke). Verified on the local stack (Postgres 17.6) after `db reset`: the five catalog queries in the file answer exactly the noted results; the anon/authenticated grants listed are Supabase's default privileges, identical on `jobs` and `operator_rate` (RLS with no policies blocks them, and the pgTAP file proves it with a real anon read of an existing row). `supabase/tests/operator_turns_test.sql` (15 tests, whole pgTAP suite 8 files pass): shape, RLS, no policies, the anon read, both checks, the claim matching once (a replay with a sentinel `started_at` changes nothing) and the record matching only a running row. Deviation: the claim/record tests were first written as data-modifying CTEs inside subqueries, which Postgres rejects; they run as top-level updates whose effect is read back.
- **3. Store + background function** (this commit). `operatorTurnStore.js` is the only module that knows `operator_turns`: `enqueueTurn` (insert queued, returns the id), `claimTurn` (ONE update filtered on id AND `status = 'queued'`, returning `user_id, messages`; one row claims, anything else is `not_queued` / `unavailable` / `bad_id`), `recordTurn` (update filtered on id AND `status = 'running'`; non-terminal kinds refused before the DB), `readTurn` (id AND user_id, `maybeSingle`), `pruneTurns` (`created_at` older than an hour). Nothing throws; ids are shape-checked before the DB is asked. `operatorTurn-background.js` (v1 `exports.handler`, the `-background` suffix): POST `{ turn_id }`; config checked BEFORE the claim (a deploy without the key claims nothing, so the row waits for the prune instead of being burnt); claim; run with `deadlineAt = entry + deadlineMs()`; record once; a throw mid-turn is recorded `failed`. Log lines: `operator turn not claimed:`, `operator turn recorded: <kind> ms=<n>`, `operator turn record failed:`, `operator turn crashed:`, plus the runner's `operator turn stopped:`. Tests: 8 background (injected claim/record/runner), 6 store unit (a fake query builder records each chain, so a dropped filter fails here), and the `[integration]` claim race against the local stack: ten clients, one queued row, exactly one claims; the SAME update without the status filter lets all ten "claim" (the proof the filter is the guard); record-once; own-rows-only reads; the hour prune. Suite 586 tests, 574 pass, 0 fail, 12 skip (the new integration test skips without the flag; with it, 17/17 across the two operator integration files). Deadline default deliberately still 8500 here: the sync endpoint runs the turn until step 4, so a 60 s default now would be a broken intermediate state.
- **4. The endpoint: start + poll** (this commit). `operatorChat.js` POST keeps the six gates (a seventh config check, the trigger origin, sits with the others before admission) and then: prune (logged, never fatal), `enqueueTurn` for the verified user, one `fetch` to `<origin>/.netlify/functions/operatorTurn-background` with `{ turn_id }` and a 5 s signal, 202 `{ turn_id }`. The row is stored BEFORE the trigger; a trigger that throws or answers anything but 202/200 abandons the row (`queued -> failed`, new `abandonTurn` in the store) and answers 503 "(the turn could not be started)"; a trigger answered 200 is logged as "background mode is not active for this deploy" and still answered 202, because the turn ran inside the trigger and the poll will find it. The origin is `DEPLOY_URL` > `DEPLOY_PRIME_URL` > `URL` > `PUBLIC_SITE_URL` (the per-deploy host first, so a preview triggers its own function; never the Host header). GET `?turn=<uuid>`: requireAdmin, `readTurn` scoped to the caller's id, 404 for unknown or foreign, 503 for a store outage, and one shape per state. CORS methods gain GET; the `netlify.toml` rule already passes GET and its query string. Deadline default 8500 -> 60000 in the runner (its comment, `.env.example:146-149` and the runner test moved with it; the docs that say 8500 are history and D-045 records the change); the operator prompt now says Gloucester (D-044's word, one cache re-warm). `test/operator-chat.test.js` rewritten: the gate cases unchanged in substance, the hand-off (store before trigger, the trigger is the only fetch, each failure path, the 200 case, the origin order), the poll (auth, scoping, the shapes), strict origin on both verbs. Suite 583 tests, 571 pass, 0 fail, 12 skip.
- **5. The admin panel polls** (this commit). `admin.html`: the send function keeps its prelude and its network-error branch; a 202 with a `turn_id` hands over to `pollOperatorTurn`, which sleeps `OPERATOR_POLL_FIRST_MS` (3 s), then polls GET `?turn=` every `OPERATOR_POLL_EVERY_MS` (2 s) until the state is not queued/running, the server answers something other than 200 (handed back to the existing branches: 401/403 session expired, 429 with its text, 404/503 with the server's line), or `OPERATOR_WAIT_CAP_MS` (90 s) passes, in which case one message, "The assistant did not answer in time. Your question is back in the box." A network blip between polls is retried, not surfaced. The ok branch is now `data.status === "done"`; stopped and failed map onto the old stopped and else branches; the waiting line says a longer question can take up to a minute. Rendering unchanged (textContent only). `test/admin-html-syntax.test.js`: the ok-branch anchor updated; a new pin reads `OPERATOR_WAIT_CAP_MS` from the HTML and asserts it exceeds `deadlineMs({})` from the runner (enforce-invariants-in-build), plus the poll URL, the 202 trigger, the queued/running loop and the timeout message. Trap hit again: the Bash tool stripped the `\d` in three regexes written through a heredoc; fixed with Edit (memory: bash-tool-strips-backslashes). Suite 584 tests, 572 pass, 0 fail, 12 skip.
- **5b. Retention backstop + the queued line** (this commit; from the review pass). The per-POST prune only runs when someone asks a question, so a transcript could sit for as long as nobody asked; `purge-handoffs.js` (the daily scheduled run) now calls `pruneTurns` after the handoff purge through a testable `runPurges(supabase, now, log)`, logging a count; a prune failure is logged and does not fail the regulated handoff purge (`test/retention.test.js`). Retention is therefore "an hour, or the next daily run at most", which is what D-045 says. The POST happy path logs `operator turn queued: <id>` so the deployed ride has a positive line in the operatorChat log, not only the absence of a `trigger answered` one. Live check: the Netlify env carries no `OPERATOR_TURN_DEADLINE_MS` or `OPERATOR_TURN_LIMIT` (names read via the API), so the 60 s default applies on deploy. Suite 585 tests, 573 pass, 0 fail, 12 skip.
- **6. Docs** (this commit). `DECISIONS.md` D-045 (the shape, the named spend defence and its residual, retention as implemented, the deadline default change, the log signatures including the two that mean background mode is off, the cost) and a "turn runtime SUPERSEDED by D-045" pointer on D-040's status line; `LESSONS_LEARNED.md` L-042 addendum (resolved by D-045; the cold duration to be added after the ride); `ROADMAP.md` line 80 ticked and line 70's stale "first invoicing ride still owed" corrected; `NEXT_SESSION.md` 2026-09-17 entry with the citations table; `docs/STRIPE_SETUP.md` carries `TODO(D-045/stripe-invoice-events)` at the webhook step until Ben confirms the events. Not `[skip ci]`: this commit will be the tip of the push and the tip must build (L-037).

## Closing walk (steps 1 to 6; step 7 is the deploy gate and stays open until the ride)

- [x] Code: runner, store, background function, endpoint, admin page, daily prune backstop. Each commit green; suite at close 585 tests, 573 pass, 0 fail, 12 skip.
- [x] Wiring: the new function file is auto-discovered (netlify.toml `functions = "server/netlify/functions"`, no registration); the existing `/api/v1/operator-chat` rule passes GET and its query string (the same mechanism `invoices.js` and `expenses.js` already use); CORS methods gain GET; no new env vars (the trigger origin comes from Netlify's own `DEPLOY_URL` / `URL`, already relied on by `origins.js`); `.env.example` updated for the new deadline default; `.gitignore` untouched.
- [x] Migration: catalog verification queries shipped in the file; run locally with the noted results; pgTAP 15/15; the hosted apply and re-verification are step 7.
- [x] Tests that can fail: the claim race carries its own negative control; the client-cap pin reads the constant from the HTML; the store's fake builder records every filter.
- [x] Docs: D-045, D-040 pointer, L-042 addendum, ROADMAP, NEXT_SESSION, the checkpoint log.
- [x] Deferred anchors: none left. The `TODO(D-045/stripe-invoice-events)` marker in `docs/STRIPE_SETUP.md` was resolved the same evening: Ben added the four `invoice.*` events in the Dashboard, the API read back exactly the five, the step was rewritten.
- [ ] Step 7: migration applied to hosted by Ben and catalog-verified; push (per-action go-ahead); deploy `ready`; the cold two-tool ride; log lines `operator turn queued:` and `operator turn recorded: done ms=`; the cold duration into L-042.
