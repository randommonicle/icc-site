# ICC Platform — Decision Records

Architectural decisions, newest-relevant kept together. Each has an ID (D-NNN), a status, the decision, and the reasoning. When a decision changes, add a new record that supersedes the old one rather than editing history.

Same convention as the ASH app and PropOS. Reference decisions by ID from code comments and other docs.

**Status:** Accepted · Proposed · Open (needs a call) · Superseded

---

## D-001 — Multi-page static front end, built with Astro
**Status:** Accepted (front-end tool resolved: Astro, June 2026)

The public site becomes a proper multi-page static site so each page is independently crawlable and can target its own search terms — the foundation of the SEO strategy (DESIGN §4, §9).

The front-end-tool sub-question is **resolved in favour of Astro** (a static site generator). Reasoning: Phase 1 is content- and SEO-heavy (Services, About, History, a growing library of care guides, and several core/wider-area pages per D-011), which is exactly where Astro's shared layouts/components and content collections remove duplication and keep per-page SEO consistent; `@astrojs/sitemap` generates the sitemap at build; output is zero-JS static HTML (fast, fully crawlable); and the existing chat widget ports cleanly as a client-side island with the `/api/*` serverless contract unchanged. The cost is a Node build step — already anticipated in CLAUDE.md, and immaterial on the project's paid Netlify plan. Hand-built HTML was the alternative; rejected because ~12+ pages would duplicate head/nav/footer and need hand-maintained meta, structured data, and sitemap, with drift being the exact failure mode Phase 1 is trying to avoid.

Implementation: scaffolded under `site/` — a small, deliberate pull-forward of the D-014 monorepo layout, so the site is not relocated at Phase 2. TypeScript strict, vanilla CSS ported from the Phase 0 site, static output, deployed on Netlify. The Phase 0 single-page `index.html` keeps serving until the Astro site is cut over in a dedicated PR.

## D-002 — Supabase (managed Postgres) for the operational backend
**Status:** Accepted

Invoicing, payments, calendar sync, a client database, and marketing automation need persistent relational data, authenticated access, and background processing. Netlify Blobs is a key-value store and is not the right home for that. Supabase provides Postgres, admin auth, file storage for job photos, and an API without running servers. Netlify Blobs remains only for the Phase 0 PoC until Phase 2 migrates bookings into Postgres. (Consistency bonus: the ASH app and PropOS already use Supabase, so the conventions transfer.)

## D-003 — API-first design; website and field app are both clients
**Status:** Accepted

The backend exposes a clean, documented set of endpoints. The website is one client; the future field app is another, consuming the same endpoints rather than needing its own backend. Plan the API surface with the app in mind from the first backend build so nothing is retrofitted. Document it as an integration contract (mirror the ASH/PropOS `INTEGRATION_CONTRACT` convention).

## D-004 — Stripe for payments; no card data stored locally
**Status:** Accepted

Stripe handles card security, so no card data is ever stored on the platform, keeping ICC out of scope for the heaviest payment-security obligations. Covers the deposit at booking and the balance on completion. True open-banking links are heavier and parked as optional/later (not needed to take deposits).

**Addendum (June 2026) — processor kept swappable; Revolut Merchant is a live alternative.** Mark is considering **Revolut Pro** (the sole-trader account) for the business banking. That does not change this decision and is compatible with it: the **card processor** and the **receiving bank account** are different layers. Two viable processor strategies, to choose at Phase 3:

- **Stripe** (default): best API/ecosystem for a custom embedded deposit-plus-balance flow, serves the website and the field app identically, and pays out to whatever bank account Mark nominates — **including Revolut Pro** (UK sort code + account number). Slightly higher UK card fees (~1.5% + 20p).
- **Revolut Merchant API**: custom checkout + Revolut Pay, ~0.8% + 2p (0.5% + 2p for Revolut Pay), next-day settlement straight into the Revolut account Mark already uses. Younger API/ecosystem than Stripe, and it ties payments to Revolut.

There is no "Stripe is built into Revolut" — they are separate (and partly competing) products; the only Stripe↔Revolut tie is **Revolut Pay** as a checkout method Stripe can offer. Open banking / "pay by bank" (e.g. via the Revolut Pay bank route) is the low-fee option worth considering for the **deposit** specifically.

**Design rule locked now:** the payment processor sits **behind our own server-side API (D-003); the website and field app never call the processor directly.** So Stripe ↔ Revolut Merchant (or adding pay-by-bank) is a server-side swap, not a client rebuild — the choice is not a one-way door, and we don't have to make it until Phase 3. Whichever is chosen, the merchant + payout accounts are **Mark/business-owned (D-009)**. *(Fees/eligibility verified against Stripe + Revolut docs June 2026; reconfirm current figures at build time — L-009.)*

**Addendum (2 September 2026) — the deposit is collected by a one-click payment link in the booking emails; dormant until Stripe is live.** The deposit that confirms a slot (above) will be collected by a secure payment link in the customer emails rather than Mark arranging it by hand: in the booking-confirmation email for an auto-confirmed booking, and in the D-027 provisional-accept email (`server/netlify/functions/bookingAction.js`) sent when Mark accepts a late-finish booking. This is where the link appears, not a new payment decision.

- **Reuses the D-026 mechanism.** The link is a hosted payment page (Stripe Checkout / Payment Link / hosted invoice) behind the same provider adapter, dormant behind `STRIPE_SECRET_KEY`. Card data never touches ICC (Stripe-hosted), so this decision's no-card-data property holds. The Stripe-vs-Revolut choice above stays open.
- **Prerequisite (the real gate): a server-derived deposit amount.** Today the deposit is the AI assistant's free-text figure, stored as a display string in `jobs.notes` ("Deposit due: £90") with the numeric `deposit_ex_vat` column left null (`server/netlify/functions/bookingsStore.js`, `TODO(slice5x/structured-pricing)`). Auto-charging on a model-emitted string is not acceptable. Before the link goes live the deposit amount must be server-derived: a server-side deposit rule (a flat figure, or a fixed % of a validated quote) or the structured-pricing work. This is the blocker, not the Stripe plumbing.
- **Consumer-law note (not legal advice).** Taking a deposit at a distance engages the Consumer Contracts Regulations 2013 (the 14-day cancellation right for distance services) and ICC's own refund terms in the booking email (full refund at 7+ days, tapering inside 7 days / 48 hours). A defined refund path is needed before switch-on; Stripe refunds are straightforward. For a PROVISIONAL booking the link is sent only AFTER Mark accepts, so money is never taken on a slot he has not agreed to; the auto-confirmed path takes it at booking, as the terms already describe.
- **Forward-compat hook shipped 2 September 2026 (branch `feat/d027-per-day-hours`).** The two email builders take an optional deposit pay URL and render a "Pay your deposit securely" button via the shared `shared/emailSnippets.js` helper (https-only, attribute-escaped). The URL is null until Stripe is live, so the emails are unchanged today and keep the "Mark will be in touch to arrange your deposit" wording. When D-026 lands, wiring is a one-line change at each call site (create the deposit Checkout Session, pass its URL); the "Mark will be in touch" line should then be revisited so it is not redundant with the button.
- **Reconciliation.** Knowing a deposit is actually paid (not just "link sent") uses the same signed Stripe webhook D-026 plans, updating the job/invoice; failed, partial and disputed payments and idempotent link creation are handled there.
- **Relationship:** extends D-004 (deposit at booking); reuses D-026 (Stripe adapter, dormant flag, webhook); the provisional-accept path is D-027. Gated on the same out-of-repo items as D-026 (Mark's Stripe account and the processor confirmation).

## D-005 — Calendar: ICS + per-booking links first, API sync later
**Status:** Accepted

The simplest reliable version is a per-booking calendar link plus an ICS file, which works with every calendar app. Full two-way Google Calendar / Microsoft Graph sync is the richer version and follows once accounts and auth are in place. Confirm which calendar Mark uses day to day before prioritising which API sync to build first (DESIGN §13).

## D-006 — AI knowledge lives in one maintainable structured source
**Status:** Accepted (implementation in Phase 1/2)

The science/history content shown on the website and the assistant's system-prompt knowledge should come from the same source so they never drift. Today the system prompt is inline in `chat.js`; as content grows, factor it into a shared structured source consumed by both the site build and the AI proxy. This is the ICC analogue of the ASH "single source of truth" rule.

## D-007 — Single source of truth for AI model names; Sonnet for text, Opus only for vision
**Status:** Accepted

The cost discipline from ASH/PropOS applies here. Sonnet handles all text; Opus is used only when the latest user message contains an image (visual understanding genuinely needed). Model names must not be hardcoded across scattered call sites — once there is a backend, route them through one config value. Already partly implemented in `chat.js`.

**Addendum (12 June 2026) — vision tracks the current best Opus; bumps are deliberate, not automatic.** The vision slot in `shared/config/models.js` is pinned to the **best available Opus** (now `claude-opus-4-8`, up from `claude-opus-4-5`). Two reasons it is worth always being on the top Opus for image analysis: high-resolution image understanding arrived with Opus 4.7 and is exactly what photo-based carpet assessment needs; and the Opus tier is **flat-priced at $5/$25 per MTok across 4.5–4.8**, so "always the best Opus" is cost-neutral within the tier (the only cost wrinkle is that high-res images consume more image tokens per photo — pennies at one photo per turn). Text stays on Sonnet (`claude-sonnet-4-6`), already the current Sonnet.

The mechanism is a **deliberate one-line pin, not auto-detection.** We explicitly rejected code that fetches "the latest" model and switches automatically: a new model can shift tone, escalation propensity, citation and refusal behaviour, and silently swapping the model that gives damage-risk advice is precisely the D-019 failure mode. Every AI change in this project is deploy-preview-verified by a human before it reaches customers (4b/4c), and a model bump is no different. So the policy is "vision = current best Opus", realised as: change the one line in `models.js` when a newer Opus lands **and** a preview photo assessment confirms it still reads well. The single-source-of-truth (this D-007) is what makes that a trivial, low-friction change while keeping the human gate. (Operational watch: Opus 4.8 defaults `effort=high` on the API; if a vision turn's latency or cost creeps up, set `effort` lower on that call.)

## D-008 — Consent (PECR soft opt-in) is a hard gate on marketing
**Status:** Accepted

Marketing email to past customers relies on the PECR soft opt-in: details collected during a sale of a similar service, every message carrying a clear opt-out. The platform records consent status per client and processes unsubscribes automatically. No re-engagement campaign ships without this (DESIGN §11). This gates Phase 4.

## D-009 — Accounts owned by Mark / the business from the start
**Status:** Accepted

Domain, Netlify, Anthropic, Resend, Stripe, Supabase, and Google Business Profile should be owned by Mark / the business from day one, so nothing needs transferring later. Build-lead access is granted on top of Mark's ownership, not in place of it (DESIGN §13).

**Addendum (7 June 2026) — hosted Supabase goes in a dedicated ICC organisation, not the build-lead's personal org.** Until now Phase 2 was local-first (no hosted project, D-002). Refined because the work machine cannot run the local Supabase stack (Docker OOM) and Slices 3+ build far faster against a hosted instance: we stand one up now, in a **dedicated Supabase organisation for ICC** ("Intelligent Carpet Cleaning"), separate from the build-lead's personal `randommonicle's Org`. The build lead is owner/admin to build and grants Mark a member role for access; org ownership and billing transfer to Mark or an ICC-controlled login later, with no data migration (a Supabase org is a clean ownership boundary). This keeps D-009's principle intact: ICC owns its data and account, build-lead access sits on top.

Rejected alternative: hosting the ICC project inside `randommonicle's Org` and adding Mark as a member. That inverts D-009 (build-lead owns, Mark visits), pools ICC's customer PII with other clients' projects under one personal login (an undocumented processor arrangement the privacy notice would have to describe), and makes ICC's backend hostage to the build-lead's personal account and billing (the continuity failure D-009 exists to prevent). Member access is not ownership.

Cost and hard rule: Supabase Pro is per-organisation, so the ICC org runs on **Free** for build/staging and upgrades to Pro at go-live (an ICC business cost). **No real customer PII on any instance under the build-lead's personal org**; the production instance holding real data must be on the ICC-owned org before launch (gates the Slice 5 cutover). Server-side secrets (service-role key, DB password) follow the secrets-server-side rule: Netlify env + local `.env`, never committed.

## D-010 — Engineering discipline inherited from the ASH app / PropOS
**Status:** Accepted

Branch off `main`, never push to `main` directly, deploy by deliberate merge (not mid-traffic without sign-off). Real services in tests, no mocks. Honest RAG-tracked, standards-referenced, phased audits. Keep the doc set (CLAUDE/ROADMAP/DECISIONS/LESSONS_LEARNED/NEXT_SESSION) current. Clean prose, bold for structure only. Recorded here so the convention is explicit for this repo, not just assumed.

## D-011 — Service area: Cheltenham/Gloucester/Winchcombe core, wider Gloucestershire with a flat £15 out-of-area surcharge
**Status:** Accepted (confirmed by Mark, June 2026; surcharge figure resolved June 2026 — flat £15 + VAT; postcode boundary resolved June 2026 — core towns + ~5-mile radius)

Cheltenham, Gloucester and Winchcombe are the core service area with no travel charge. ICC also covers the wider Gloucestershire area — Stroud, Tewkesbury, Cirencester, and surrounding towns (all GL postcodes). Jobs outside the core area incur a **flat £15 + VAT out-of-area surcharge** (confirmed by Mark, June 2026). The AI assistant and the site (area pages, services page, home-page strip) now state this concretely, and the assistant includes the £15 + VAT in the itemised quote for out-of-area addresses, shown up front. This shapes the SEO area pages (D-001): Cheltenham/Gloucester/Winchcombe are core/no-surcharge pages; Stroud/Tewkesbury/Cirencester and the wider GL towns are wider-area pages stating the flat £15 + VAT. **Boundary resolved (Mark, June 2026):** the core no-surcharge area is Cheltenham, Gloucester and Winchcombe plus roughly a 5-mile radius around each; everywhere else carries the surcharge. Encoded in `shared/config/serviceArea.js` as the fully-core GL50–53 / GL1–4 districts (each town and its ~5-mile ring) plus the **GL54 5 sector** for Winchcombe, with the rest of GL54 (Bourton GL54 2, Stow GL54 1, Northleach GL54 3 — far Cotswolds) out of area. "5 miles" is a radius and the check works on postcode districts, so this is a documented close approximation; `isOutOfArea()` and the assistant prose both read it and err toward charging on a borderline or ambiguous postcode. **Still open (Phase 2):** server-side *enforcement at booking* — `validateBooking` still only bounds the total price, so the surcharge is computed in the assistant's quote and `/api/v1/quote` but is not yet enforced when a booking is written. Wire `isOutOfArea` into booking validation when pricing/area logic moves server-side (D-007 / Slice 5), so the website and the future field app (D-012) apply it identically.

## D-012 — The field app is built concurrently with the website and fully integrated, not deferred
**Status:** Accepted (confirmed by Mark, June 2026)

The field app has not been designed yet, but Mark wants it built alongside the website and fully integrated rather than treated as a final, separate phase. This raises the priority of the API-first design (D-003): the backend API surface must be designed from the first backend build (Phase 2) to serve both the website and the app as equal clients, with a documented integration contract. The app is no longer "Phase 5 only" — it is a parallel track that consumes the same API as it matures. Practical consequence: front-load API design in Phase 2, keep all business logic server-side (never only in the website client), and version the API/contract from day one so the two clients never drift. Supersedes the roadmap's earlier framing of the app as a deferred Phase 5 item.

## D-013 — Domain registration is deferred but non-blocking
**Status:** Accepted (June 2026); **resolved 7 June 2026 — `intelligentclean.co.uk`**

The live domain is not yet chosen. This does not block Phases 0–2: development continues on the Netlify-assigned URL, and `ALLOWED_ORIGINS` / Resend / Google Business Profile / structured data are configured once the domain is registered. Recorded so the open question is visibly parked rather than forgotten. Must be resolved before the SEO/findability launch (Phase 1 go-live) and before `ALLOWED_ORIGINS` strict mode (L-001).

**Resolved (7 June 2026):** the domain is **`intelligentclean.co.uk`**, registered with 123reg (1 year), DNS propagating (123reg quoted 24–48h). Chosen for brevity and brand fit; it deliberately omits "carpet" and "cheltenham" (a weak SEO signal now, and "clean" future-proofs for upholstery and other services). Unblocks, before promotion and when convenient: `ALLOWED_ORIGINS` strict mode in Netlify (`https://intelligentclean.co.uk,https://www.intelligentclean.co.uk`, closes L-001); updating the now-stale hardcoded fallback origins in `chat.js` (they guessed `intelligentcarpetcleaning.co.uk`); locking `astro.config.mjs` `site` for the sitemap/canonicals at cutover; and verifying a Resend sending domain (L-004). 123reg DNS points at Netlify at go-live. None of this blocks the Phase 2 Supabase build.

## D-014 — One monorepo for the whole ICC platform (site + app + backend + shared contract)
**Status:** Accepted (June 2026)

The public website, the field app, the backend/serverless functions, and the shared API contract all live in this single repository. This is the natural consequence of D-003 and D-012: the website and the field app are two clients of one product, not two products.

Reasoning:
- One product, one repo. This is the dividing line from the ASH↔PropOS strategy, where separate repos are used precisely because those are separate products with separate commercial lives. The ICC site and ICC app are clients of the same platform, so they belong together.
- Single source of truth for the API contract. A `shared/` package (endpoint shapes, types) is imported by the site, the app, and the functions, so the compiler enforces D-012's "the two clients never diverge". Across two repos this needs a published versioned package or hand-duplication, and drift is exactly the failure mode we are avoiding (it is why PropOS needs the XR cross-repo tagging convention).
- Same win for D-006: the AI knowledge source lives in `shared/` and is consumed by site content, the assistant prompt, and the app.
- Bus factor: one clone, one doc set, one decisions/lessons log covering the whole platform.
- Atomic changes: an endpoint change plus both clients land in one PR, not coordinated cross-repo PRs.

The one real cost is two deploy targets (site → Netlify, app → Capacitor build). Handle it by scoping Netlify's build to changes under `site/` (and `shared/`) so an app-only commit never triggers a site deploy, and vice versa. The ASH repo already proves a Capacitor app + server + Supabase migrations coexist in one repo.

Target layout (created when Phase 2 starts, before the API exists to consume):
```
icc-platform/
├── site/                       public multi-page static site
├── app/                        field app (Capacitor)
├── functions/ (or server/)     API + AI proxy + serverless
├── shared/                     API contract types + AI knowledge source (D-006, D-012)
├── supabase/migrations/
└── docs/ + CLAUDE.md, ROADMAP.md, DECISIONS.md, LESSONS_LEARNED.md, NEXT_SESSION.md at root
```

Sessions stay separate by working on branches (existing discipline, D-010) or git worktrees — the monorepo makes separate site/app sessions easier, not harder, because each has the shared contract and docs to hand. The repo is currently named `icc-site`; rename to `icc-platform` when convenient (GitHub redirects the old URL) or leave it as a cosmetic mismatch. Do the monorepo restructure before building the Phase 2 API, so the contract is born in `shared/` rather than extracted later.

**Addendum (5 June 2026 — implementation):** backend folder resolved to **`server/`** (not `functions/`). Phase 2 Slice 0 moved `chat.js`/`bookings.js` to `server/netlify/functions/` (history preserved) on `chore/d014-monorepo`, updated the `netlify.toml` `functions` path, and repointed the test require paths — `node --test` green 8/8 locally, awaiting sign-off, not merged. Two things were deliberately deferred to keep the structural change minimal and reduce live-deploy risk: **npm workspaces + a `server/package.json`** (introduced when `server/` first needs its own dependency — e.g. `@supabase/supabase-js` in Slice 2; until then the functions resolve root `node_modules` exactly as before, so Netlify's install/bundle is unchanged), and the **build-scope `ignore` directive** (no consumer until `app/` has a real build, and Phase 0 still publishes the repo root incl. the live `index.html`/`admin.html`, so it needs care — better introduced with the site cutover). `shared/` and `supabase/migrations/` are created in their own slices.

## D-015 — No quantified trust or marketing claims until independently substantiated
**Status:** Accepted (June 2026)

The public site must not show quantified track-record or trust metrics (completed-job counts, satisfaction percentages, star ratings) or superlative claims ("first", "guaranteed") unless each is independently substantiated and current. ICC is a new business with no track record yet, so figures like "500+ cleans" or "98% satisfaction" are unverifiable and breach ASA/CAP and the L-009 rule on repeating unverified claims. Capability facts that are simply true (for example "15+ carpet types", the six trading days, the 10% deposit) are fine, because they describe what the service is rather than a track record it has not earned.

Where a quantified metric would genuinely add value, it goes in only once there is a real, defensible source: a true completed-job count, or a verified review aggregate (which would also back an `aggregateRating` in the structured data). Implemented June 2026 on `feat/pre-cutover-polish`: the fabricated `500+`/`98%` tiles on the home and About pages were removed and replaced with capability facts, the "Guaranteed Results" and "first fully AI-assisted" claims were softened, and a grep-able `TODO(trust-stats)` anchor marks where substantiated metrics may later go. This is the site-side companion to L-009 (which governs the assistant) and is a hard gate on promoting the site off the Netlify name.

**Addendum (24 August 2026) — brand copy confirmed with Mark.** The homepage tagline is **"Intelligence you can trust"** (replacing "Established Trust, Superior Cleaning", which implied a track record a new business has not earned, the exact D-015 risk). The About page says **"local specialist"** (replacing "a local team"; Mark is a sole operator). Experience is stated as **"over 15 years"**: Mark asked for "18 years", but the true figure is 16, and an inflated figure is a false claim that breaches this decision and ASA/CAP, so the honest "over 15 years" is used. All three are true-capability statements, which D-015 permits, not unverifiable track-record claims. Recorded from the meeting minutes (D2/D3/D4).

## D-016 — Registered address kept private; ICC runs as a service-area business
**Status:** Accepted (5 June 2026)

ICC's registered business address is **11 Horsbere Road, Hucclecote, Gloucester, GL3 3BT** (inside the Gloucester core area — GL3 — so no surcharge applies to its own neighbourhood). The address is kept **private**: used only for Google Business Profile verification and the data-controller / privacy layer, never published on the public site or in NAP / structured data. ICC is set up as a **service-area business** (it travels to the customer), so the Google Business Profile is configured with service areas and the street address hidden.

Reasoning (the SEO question that prompted this): for a home-based service-area business, displaying the street address gives at best a marginal SEO benefit and is not worth the privacy/safety trade-off. Google ranks on the **verified** address (given privately for GBP verification), not the **displayed** one, so hiding it costs ~nothing in proximity ranking; Google's own guidance is that service-area businesses should not display an address they don't serve customers at. The real local-SEO levers are unaffected: a verified GBP + correct category + service areas, the review engine, NAP (here Name + Phone) consistency, the area pages, and structured data. The one useful move is publishing the **locality** (Hucclecote / Gloucester / GL3) in content, not the house. Practical consequence: this address fills the privacy-notice data-controller `[to confirm]` slot only (still gated on Mark + a DP review); it does **not** go on the contact page or into `LocalBusiness` structured data.

**Addendum (18 June 2026) — sole trader confirmed; the public privacy notice carries no address.** Mark confirmed his legal status as a **sole trader**, so the controller is "Mark McClymont, a sole trader trading as Intelligent Carpet Cleaning" (there is no company number). Reviewing D-016 against the fact that the address is Mark's *home*, the public privacy notice was rewritten to identify the controller **without** publishing the address: contact is by email, phone, and **post on request**, and the full address is given to the **ICO privately** (which sole traders can ask to keep off the public register, see `docs/LAUNCH_ICO_REGISTRATION.md`). This takes the privacy-protective reading of D-016 ("kept private ... never published on the public site") and supersedes the literal "fills the privacy-notice slot" line above: the address now fills the **ICO registration and Google Business Profile verification** uses only, never the public web page. The GBP profile likewise hides the address (`docs/LAUNCH_GBP_PROFILE.md`). The notice's only remaining controller `[to confirm]` is the ICO registration number.

**Addendum (24 August 2026) — ICC's public phone is a dedicated virtual 01452 landline (confirmed).** The phone-number question that had gated the GBP is settled. ICC uses its **own virtual Gloucester 01452 landline, call-forwarded to Mark's mobile**, kept separate from Regency's 01242 279590. Start with a cheap number (about £1.99/mo for the number, about £5/mo with forwarding) and upgrade to a memorable one later. Because the address is hidden, the phone is ICC's main public NAP identifier, so it must be ICC's own. Provisioning is action A1 (Ben), and it unblocks the GBP. Provider options and the number-rental recommendation are in [docs/LAUNCH_GBP_PROFILE.md](docs/LAUNCH_GBP_PROFILE.md).

## D-017 — Node runtime pinned to 24 (build + Functions)
**Status:** Accepted (5 June 2026)

Node 20 reached end-of-life on 30 April 2026, so the platform pins **Node 24** for both the Netlify build and the serverless Functions runtime. Node 24 is the active LTS line (security support to April 2028) and is usable on Netlify Functions because AWS Lambda shipped the `nodejs24.x` runtime on 25 November 2025 (Functions inherit the build's Node version when it is a valid Lambda runtime, otherwise they fall back to Node 22).

Pinned in three places, with `NODE_VERSION` taking precedence: `netlify.toml` (`[build.environment] NODE_VERSION = "24"`), root `package.json` (`"engines": { "node": "24" }`), and `.nvmrc` (`24`). Verified end to end before relying on it: the PR #9 deploy preview built and ran the functions on Node 24, and the published production deploy is the Node 24 merge (`a7131f2`, confirmed via the Netlify CLI `listSiteDeploys`). Landed standalone on `chore/node-version` (PR #9 to `main`) ahead of the Phase 2 stack, so the EOL runtime was not carried into new backend work. The Phase 2 branches (#6 to #8) were cut before this merge and still pin Node 20; they must resolve to keep Node 24 when they merge (their `netlify.toml` and `package.json` conflict on the version — resolution: keep the `server/...` functions path and Node 24).

## D-018 — Email: Google Workspace for mailboxes, Resend for transactional, both on intelligentclean.co.uk
**Status:** Accepted (10 June 2026)

ICC's email runs on two services over the one verified domain, set up this session:

- **Human mailboxes** (send + receive) on **Google Workspace Business Starter**. The first/admin account is `ben@intelligentclean.co.uk` (build-lead super-admin, recovery to Ben's personal Gmail); the owner mailbox is `mark@intelligentclean.co.uk` with free aliases `hello@`/`info@`/`bookings@`. Ben administers because Mark is non-technical — the same build-lead-operates / business-owns split as D-009. Cost note: two licensed seats currently; before the 14-day trial converts, `ben@` can move to **Cloud Identity Free** (admin, no mailbox, no charge) so only `mark@` is billable, and `mark@` should be given an admin/recovery path so the business is never locked out (bus factor).
- **Transactional/automated email** (booking confirmations now; Supabase Auth from Slice 5) via **Resend**, sending from the verified domain. The app's From-addresses are env-driven in Netlify (`CUSTOMER_FROM=hello@…`, `OPERATOR_FROM=bookings@…`, `OPERATOR_EMAIL=mark@…`).

There is no "link the domain to Supabase" step: Supabase does not host mailboxes — it only *sends* auth email over SMTP, which from Slice 5 points at the same Resend domain. The two sending paths stay independent by DNS design: Google's SPF/DKIM on the root, Resend's on the `send` subdomain, one shared DMARC (`_dmarc`, `p=none` during rollout). Verified end-to-end with a live booking and closes the L-004 sending-domain item. DNS lives at 123reg (GoDaddy `*.domaincontrol.com` nameservers); the web records (→ Netlify) are deliberately untouched until the Phase 1 cutover.

**Addendum (11 June 2026) — the owner mailbox is `mark_director@`, not `mark@`.** The address stated above is wrong: Mark's Workspace account was created as `mark_director@intelligentclean.co.uk`, and `mark@` was never provisioned (the Directory shows only `ben@` and `mark_director@`). `OPERATOR_EMAIL` is therefore `mark_director@intelligentclean.co.uk` (corrected in Netlify this session). The `hello@`/`info@`/`bookings@` aliases belong to Mark's account (`mark_director@`), not to a `mark@` mailbox. The original `mark@` value had silently hard-bounced and been Resend-suppressed since 10 June, dropping every operator email including booking notifications — see **L-015**. The 10 June "verified end-to-end" had only confirmed the *customer* leg. **Follow-ups:** confirm `hello@`/`bookings@` exist as real aliases so customer *replies* to those From-addresses don't bounce (sending already works via the verified domain); and revisit which Netlify account holds the site — currently the build-lead's *Personal* account (D-009 ownership).

## D-019 — AI assistant grounding, source hierarchy, and human escalation
**Status:** Accepted in principle (11 June 2026); implementation in Phase 1 (knowledge consolidation) + Slice 4 (backend AI / handoff)

The assistant must not give incorrect information — above all, aftercare or stain advice that could physically damage a customer's carpet. The agreed design is a **grounded assistant with a two-lane source hierarchy and a safe "I don't know → escalate to a human" exit**, built as layered (defence-in-depth) guardrails rather than a single prompt instruction. Rationale: the model has no calibrated confidence, and an AI/web summary is exactly what caused the L-009 drift — so escalation is engineered by **category**, not by trusting the model to police itself.

**Source hierarchy (two lanes):**
- *Low-stakes / general* questions: vetted KB (D-006) → attributed web search → escalate.
- *Damage-risk / out-of-scope* questions: vetted KB only → **escalate to a human immediately** (never fall back to web for advice that could damage a carpet).

**Claude mechanisms (verified against the API, June 2026):**
- **Citations** — pass the vetted content as documents (or the cached system prompt, L-002) so Claude grounds and cites each claim to its source; an uncitable claim is itself a signal to soften or escalate.
- **Server-side `web_search`/`web_fetch`** — used only for low-stakes gaps, always **attributed** ("the manufacturer states…"), never asserted as ICC fact (L-009).
- **A custom `escalate_to_human` tool** — the model calls it when out of its depth; the *description must state when to call it* (out-of-KB, damage-risk, or no citable source), because recent Claude models under-reach for custom tools without explicit triggers. It returns the "I'll get the team to check" message and captures the question + contact as a lead.
- Constraint: Citations and forced-JSON structured outputs cannot be combined in one call, so prefer Citations + the escalation tool over a model-reported confidence score (which is unreliable anyway).

**Structural guards:** bound the assistant's remit to *educate + quote + book* (defer prescriptive treatment of valuable/delicate items to a professional assessment — escalate by design); optionally a cheap second-model (Haiku) verification pass that checks a safety-relevant draft against the KB before it is sent.

**Where it lands:** the build-out of D-006 (one vetted source feeds site + assistant) and the enforcement of L-009. The human-handoff half uses the **Slice 4 `messages` table** (draft → Mark approves → send) + admin review, so escalations become logged leads and Mark's corrections feed back into the KB. Today `chat.js` already does the simplest tier-1 (a cached system prompt = in-context grounding); value-ordered upgrade path: **structured KB → Citations → gated web tool → escalation tool + verification.**

**Addendum (11 June 2026) — three refinements settled while building Slice 4 (PRs #16/#17):**

- **No Haiku in the AI path.** The optional second-model (Haiku) verification pass is dropped. Using a model to police a safety-critical draft is exactly the un-calibrated-confidence problem D-019 exists to avoid; the engineered guards — Citations grounding (every claim tied to a source), the category-based `escalate_to_human` exit, and a bounded remit — are the defence-in-depth. If a second-model check is ever wanted it would be Sonnet, not Haiku, but the call is to leave it out. Supersedes the "optionally a cheap second-model (Haiku) verification pass" note above.
- **Citations are shown to the customer as links.** The Citations API returns, per grounded sentence, the source it came from. Rendered safely (DOM `<a>` via `createElement`, sanitised `https` href, never `innerHTML` — preserves L-003): **KB-grounded** claims link to ICC's own care-guide pages (best once the Phase 1 site is live so the guide URLs exist; a plain source label until then); **web-search** claims render as attributed external links ("the manufacturer states… [source]", satisfying L-009). Built in Slice 4c; adds a small client-renderer change to `index.html`, which today reads `data.content[0].text` and must be extended to render the citations payload (see L-014).
- **All platform AI runs on the Anthropic API; a consumer Claude.ai subscription cannot power it.** A Pro/Max subscription authenticates a *human* in the Claude app and issues no API key, so neither the chatbot nor the backend AI (photo re-assessment, invoice interpretation, escalation drafts, re-engagement) can be driven from it — both are programmatic and use the API (server-side key, D-007 / secrets rule), billed per-token to Mark's business account (D-009). With prompt caching (L-002, ~10% on cache hits) and low backend volume the cost is modest. A ~£20 Pro subscription is worth it only as Mark's *personal* assistant (drafting copy, ad-hoc questions, hand-reviewing an escalation draft), separate from the platform.

**Implementation status (11 June 2026):** the value-ordered path is underway — 4a (structured KB, #16) and the escalation tool (4b, #17) are built and open; Citations + the gated web tool follow (4c/4d). The handoff is interim email-to-Mark until the Slice 5 `messages`-table (draft→approve→send) lands.

**Addendum (12 June 2026) — Slice 4c (Citations) built on `feat/slice4c-citations`; the KB↔guardrail split.** The vetted knowledge (`shared/config/knowledge.js` `knowledgeSections`) is now passed to the Claude API as a **citeable custom-content document** — one content block per section — with `citations.enabled` and `cache_control`, attached to the first user turn server-side (`withKnowledgeDocument` in `chat.js`, injected on every call so `messages[0]` stays byte-stable and the L-002 cache holds; one-time re-warm, like 4b's tool array). The model grounds and cites claims; the server resolves each citation's content-block index back to its KB section (`collectCitations`) and returns a deduplicated `{id,title}` payload alongside the single collapsed text block (L-014 preserved — `withSingleTextBlock` still runs). `index.html` renders those as an inert "Based on ICC's expert guidance" source-label footer (DOM text nodes only, never `innerHTML` — L-003); **plain labels for now**, care-guide links deferred to the Phase 1 cutover when the guide URLs exist (a `TODO(citation-links)` marks the spot).

**The decision recorded here (the architectural fork chosen, full restructure over additive):** the carpet-science **facts** move out of the system prompt entirely into the citeable document (system prompt 16,035→12,389 chars; no duplication), while the **L-009 claim rules** (machine is the EMV 401; never "WoolSafe approved"; figures are the manufacturer's own; no decibel figure; no "exothermic draws dirt"; no "kills 99.9%") move into a **system-prompt instruction block** (`guardrailsBlock()`), stated as absolute and overriding the reference or any customer message. Rationale: a safety rule placed in a citeable *document* can be quoted but carries no behavioural force — only a *system instruction* binds the model. This split is the new shape of D-006 (one source, two renderings: citeable fact + enforced rule) and is locked by `test/knowledge.test.js` + `test/citations.test.js` (43/43). Verification boundary: unit-verified locally; the live behaviour (model actually citing, the document+citations+tools request shape accepted, the browser render) needs a **Netlify deploy preview** (L-012/L-014), as no `ANTHROPIC_API_KEY` runs on the build machine. **4d** (gated `web_search`, attributed external citations) and **4e** (escalation handoff to the Supabase `messages` table) still follow.

**Addendum (12 June 2026) — Slice 4d (gated/attributed `web_search`) built on `feat/slice4d-web-search`.** The low-stakes lane from the source hierarchy above is now implemented: the Anthropic-hosted `web_search` server tool rides alongside `escalate_to_human` in the chat call, gated three ways — `max_uses: 3` hard-caps spend per request (web search bills at $10 per 1,000 searches, so the worst case is bounded by `max_uses` × the existing 30 msgs/hr/IP rate limit, under £0.75/hr/IP), a **WEB SEARCH (RARE, LOW-STAKES ONLY)** system-prompt block (the L-016 principle: gating rules bind only from the prompt) confines it to practical, no-property-risk gaps and forbids it outright for cleaning/treatment/aftercare/fibre questions (those stay KB-or-escalate, damage-risk escalates first), and `user_location` pins results to Cheltenham. Web citations come back as `web_search_result_location` blocks; `collectCitations` maps them to `{id:null, title, url}` (http/https enforced server-side, deduped by url) next to the unchanged `{id,title}` KB entries, and `renderCitations` in `index.html` renders them as a muted "Looked up on the web" caption of real external links (DOM `createElement` + `setAttribute`, href re-checked against `^https?://`, `target="_blank"` + `rel="noopener noreferrer"`, never `innerHTML` — L-003), satisfying the attributed-external-links half of the 11 June citations decision. `runAssistantTurn` gained the `pause_turn` continuation (append the assistant content verbatim and re-call, no tool_result — the slot the 4b comment reserved), sharing the existing `maxRounds` bound, and `withSingleTextBlock` now leaves a paragraph break where a dropped non-text block sat so search narration never glues to the answer (a deliberate contract change, updated in `test/escalation.test.js`).

**Tool version pinned to `web_search_20250305`, deliberately not the newer `web_search_20260209`:** dynamic filtering (20260209) runs extra code-execution rounds to post-process search results, adding latency a live customer chat does not want, and our capped low-stakes lookups gain nothing from the accuracy boost — that latency reason is load-bearing. (20260209 also historically had narrower model support: it did not cover the then-current vision model `claude-opus-4-5`.) Revisit only if a future use case actually wants dynamic filtering. Two ops notes: **web search must be enabled at organisation level in the Anthropic Console** (Settings → Privacy) for the account behind `ANTHROPIC_API_KEY`, or the tool errors; and adding the tool to the tools array is a **one-time L-002 cache re-warm**, exactly like 4b. Multi-turn note: the client keeps only the collapsed plain-text reply in history (L-014), so `encrypted_content` from search results is never passed back — that simply means a later turn cannot re-cite an earlier turn's search results and searches afresh instead, which is the behaviour we want. Verification boundary: unit-verified (`node --test` 62/62); live behaviour (model actually searching on a low-stakes question, refusing to search on damage-risk, link rendering) needs a **Netlify deploy preview** (L-012), as no `ANTHROPIC_API_KEY` runs on the build machine. **4e** (escalation handoff to the Supabase `messages` table) remains.

**Addendum (24 August 2026) — no separate chat mascot.** Mark floated a Clippy-style helper mascot ("Ruggy", a carpet with eyes). Decision: do not add one. The single existing chat helper icon already routes to this assistant; a separate mascot would be the *same* assistant under a second persona, and because the assistant presents as a named human (Jamie/Alex/Sam/Ellie/Tom, per `chat.js`), a mascot voice would collide with that. A separate API key or token just for a mascot is extra cost and bloat for no gain. Keep one assistant and one entry point. Recorded from the meeting minutes (D10 and section 7).

## D-020 — Escalation handoffs are `human_handoff` messages (customer-optional), reusing the messages table
**Status:** Accepted (Slice 4e foundation, June 2026)

The `escalate_to_human` handoff (Slice 4b, today emailed to Mark) becomes a row in the `messages` table so it shares the draft → approved → sent lifecycle and the future admin review surface, instead of living only in Mark's inbox. This realises the "human-handoff half uses the Slice 4 `messages` table" note in D-019.

- **Reuse `messages`, not a separate leads/escalations table.** The approval workflow, the admin surface and the status lifecycle (`draft`/`approved`/`sent`/`failed`/`suppressed`) are shared, so a handoff is just another AI-originated message Mark reviews. A `human_handoff` value is added to the `message_kind` enum in its own migration (isolating the `ALTER TYPE ... ADD VALUE`); the follow-on migration makes `customer_id` nullable and adds the check, comparing `kind::text` so it never depends on the new enum value being committed in the same transaction.
- **`messages.customer_id` is now nullable, constrained so only a `human_handoff` may omit it** (`messages_customer_or_handoff`). An escalation is a lead with no customer row yet; forcing a `customers` row would pollute the unique-email table with non-customers and can fail (its `name`/`phone`/`email` are `NOT NULL`). Every outbound customer comm still requires a customer. The lead's name, contact and question live in the message `subject`/`body` for now; structured columns can follow if the admin UI needs them.
- **v1 captures the handoff; it does not draft a customer answer.** The draft row records reason, contact, question and a short transcript for Mark. Generating a tentative *answer* is a later layer and must **never** run for `damage_risk` (the assistant escalated precisely because it must not answer); it would apply only to softer reasons (`out_of_scope`/`no_citable_source`) and always as a Mark-approved draft.
- **Foundation only; live wiring deferred to Slice 5.** `shared/messages.js` (`escalationToMessageDraft`) builds the row and is unit-tested; the migration is pgTAP-verified locally. The live path is unchanged: `chat.js` still emails Mark, does not touch Supabase, and gets no new secret. Wiring the function to `INSERT` with the service role, plus the admin review/approve/send UI, land with the Slice 5 cutover (sign-off-gated). This is why D-019 said 4e "couples with Slice 5".

**Addendum (14 June 2026) — Slice 5a: the escalation INSERT is wired live (fail-open, env-gated).** `handleEscalation` in `chat.js` now ALSO inserts the `escalationToMessageDraft` row into the Supabase `messages` table via a service-role client (`server/netlify/functions/supabaseClient.js` `getSupabaseAdmin()`), in addition to emailing Mark. Three properties make this safe to merge ahead of the rest of the cutover:

- **Fail-open.** The INSERT is wrapped so a Supabase outage, an RLS/constraint reject, or missing creds is logged and swallowed — it never throws and never blocks the customer reply. The email to Mark remains the guaranteed path (L-004).
- **Env-gated, so the merge is a production no-op until deliberately enabled.** `getSupabaseAdmin()` returns null unless BOTH `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are set; with no creds the handler is byte-for-byte the old email-only behaviour. Setting the two vars in Netlify is the single action that turns the backend on (the sign-off moment), not the merge itself.
- **Root dependency, not the npm-workspaces restructure.** `@supabase/supabase-js` was added to the root `package.json` (like `@netlify/blobs` / `pdfkit`), keeping Netlify's install/bundle behaviour unchanged. The deferred `server/package.json` + workspaces step (D-014 addendum) stays deferred — it does not belong on a live-behaviour slice.

Verified against **real local Supabase** (Docker), not just fakes: a guarded integration test (`test/handoff-insert.test.js`, run with `ICC_SUPABASE_IT=1`) inserts a real `human_handoff` row with a null `customer_id` (the `messages_customer_or_handoff` constraint permits it), reads it back, and deletes it. `node --test` is 75 pass + 1 skip (the integration test self-skips without the env flag). The remaining Slice 5 sub-slices — **5b** bookings Blobs→Postgres, **5c** the 09:00–16:30 hours, **5d** Supabase Auth, **5e** the admin handoff approve/send UI — have grep-able `TODO(slice5x/...)` anchors planted at their landing points. Hosted enablement was completed on 14 June 2026: the two 4e migrations were pushed to the hosted project (`supabase db push`, migration history recorded) and `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` were set in Netlify; a live damage-risk escalation against production wrote a real `human_handoff` row to hosted Supabase (`customer_id` null, verified then deleted), so 5a is now active in production. See L-018 for the Netlify env-var API gotcha hit while doing it.

**Addendum (15 June 2026) — Slice 5e-2: the handoff draft → send lifecycle (merged + live, PR #42 `4917464`; the `20260614110000` migration applied to hosted).** The read-only 5e queue gains actions in the admin: Mark can have a reply **drafted** by Claude, edit it, and **send** it to the customer by email, or **mark a handoff handled**. This is a customer-facing AI surface plus an outbound customer email, so it inherits the platform's AI-surface discipline (input minimisation / output discipline / human gate). Ben chose the full draft+send over status-triage-only.

- **The damage_risk gate is structural, not textual.** A migration (`20260614110000`) adds nullable `messages.handoff_reason / customer_contact / handoff_question / draft_reply`; `escalationToMessageDraft` now populates the first three. The draft action gates on `isDraftableReason(handoff_reason)` reading that column, so a `damage_risk` handoff (and `customer_request`, and any legacy null-reason row) is **never** auto-drafted — the assistant escalated precisely because it must not answer, so the model is not even called for those. Mark may still type and send a reply himself; the AI never drafts one for them. `isDraftableReason` allows only `out_of_scope` + `no_citable_source`.
- **Input minimisation (UK GDPR Art.5(1)(c)).** The draft generator (`draftReplyForHandoff`, Sonnet) receives ONLY the captured question (`handoff_question`), never the customer name / contact / transcript that live in `body`. It is grounded in the vetted KB (`knowledgeBlock`) and bound by the L-009 claim rules (`guardrailsBlock`), and is told to decline and defer to Mark when the KB does not cover the question.
- **Human gate + fail-closed send.** Drafting and sending are separate POST actions on `/api/handoffs`; the model never sends. The send fires only when a real email is extractable from `customer_contact` (`extractEmail`), and the row is marked `sent` ONLY after Resend accepts it (an unsent "sent" is L-008/L-004). Every `update` is hard-guarded to `kind='human_handoff'`, so the endpoint can never mutate a `booking_confirmation` or other customer comm. Admin output is inert (L-003): values `esc()`'d, the draft set via `.value` not `innerHTML`.
- **Additive + safe.** `bookings.js` and the live `chat.js` escalation write are unchanged in behaviour beyond populating the new columns; no new dependency. `node --test` 127 pass / 3 skip, with the damage_risk and fail-closed invariants pinned by tests; pgTAP adds the column checks. **Still to do before merge:** the independent code-review pass (cut short by a session limit), deploy-preview verification of a real draft + a real send + the damage_risk no-draft case (L-012), and the real-Supabase IT at home. **Enablement ordering:** apply the `20260614110000` migration to hosted BEFORE deploying the code (the live 5a insert now writes the new columns; it is fail-open, so nothing customer-facing breaks if the migration is late, but handoffs would not log until it lands).

## D-021 — Bookings cutover to Postgres (Slice 5b): env-gated, fail-CLOSED, hours folded in, admin reads both
**Status:** Accepted (Slice 5b, June 2026; built local-first + real-Supabase verified, enablement sign-off-gated)

`confirm_booking` and `check_availability` move off Netlify Blobs onto the Supabase `jobs`/`customers` schema (D-002): a confirmed booking upserts a customer (by email) and inserts a `jobs` row with `status='booked'`; availability is derived from the committed jobs; double-booking is the DB exclusion constraint (`jobs_no_double_booking`), not the Phase 0 read-then-write. This is the riskiest slice — the only one that changes live customer-facing behaviour — so it follows the 5a discipline (additive, env-gated, merge-is-a-no-op) with one deliberate inversion. Three load-bearing properties:

- **The booking write is FAIL-CLOSED — the deliberate inverse of the 5a escalation path.** 5a fails *open* because the email to Mark is the guaranteed channel. For a booking, **persistence *is* the deliverable**: a "Booking confirmed" email with no stored row is exactly the L-008 silent-vanish failure. So `handleBooking` persists **before** any PDF/email; the exclusion conflict (`23P01`) returns the existing `409`, and any other write failure returns `502` with **no email sent**. The client already surfaces both as its phone-number fallback (`index.html`), so no client change was needed. There is **no fallback to a Blobs write** on failure — that would reintroduce the dual-store split-brain this slice removes.
- **Env-gated by a single switch, so the merge is a production no-op.** `bookingsStoreIsPostgres()` (`BOOKINGS_STORE === "postgres"`) is the *one* source for store + availability + validation bounds, so they can never desync. Unset (or any other value), every path is byte-identical to the Phase 0 Blobs behaviour (the Blobs block is kept verbatim in the `else`). Enabling is a deliberate env set + redeploy in Netlify (the sign-off moment; env bakes at deploy, L-018); **rollback is unsetting it**. Note this differs from 5a's gate (`SUPABASE_*` presence), which is already set in production for handoffs — bookings need their own flag so they do not flip on at the 5a creds.
- **The 09:00–16:30 hours are folded in (the former Slice 5c) under the same flag.** The `jobs` schema enforces the confirmed trading day (start 9..15, every job ends by 16:00, ≤7 slots). `validateBooking(b, {latestStartHour,latestEndHour,maxSlots})` defaults to the Blobs bounds (17/18/9, byte-identical) and is called with 15/16/7 under Postgres, so a too-late/too-long slot is a clean `400` at validation, never a DB constraint error (`23514`) at insert. Availability uses the 9..15 grid under the flag. Store and hours therefore flip — and roll back — together.

Further decisions recorded:
- **No SQL RPC; two calls (customer upsert → job insert).** The exclusion constraint already makes double-booking impossible inside the single job insert, so the only gap an RPC closes is a rare orphan `customers` row when the job insert fails after the upsert — benign (a real prospect, holding no slot, deduped by the unique email next time, never touching the consent columns). Not worth a migration + pgTAP + a SECURITY DEFINER surface pre-launch.
- **No backfill; "fresh start, admin shows both".** Given near-nil real bookings, new bookings go to Postgres and the admin dashboard reads **both** stores and merges (dedupe by id — Postgres uuids and Blobs numeric ids are disjoint — newest-first), so legacy Blobs bookings stay visible. The Blobs read retires in a later tiny PR. (Verify Blobs holds no real *future* bookings before enabling; if it does, backfill them first.)
- **ex-VAT / deposit numerics stay NULL; `price_display` is authoritative.** The `BOOKING_READY` payload carries only a display string ("£475 + VAT") and the deposit is an inc-VAT figure (`pricing.js`), so parsing them into the `*_ex_vat` columns would store wrong-semantics numbers that later feed invoicing. The known surcharge (`out_of_area_surcharge_ex_vat` 15/0) *is* set. The deposit display is stashed in `notes` and recovered for the admin view. `TODO(slice5x/structured-pricing)` marks where the numerics get populated once the booking carries structured line items (e.g. via `/api/v1/quote`).
- **`jobs.postcode` made nullable** (its own migration + pgTAP + an inline catalog verification query) because the Phase 0 flow treats postcode as optional; the write resolves it best-effort (captured → regex-from-address → null). This is also where **D-011's out-of-area flag is finally derived + persisted server-side at booking** (`serviceArea.isOutOfArea`, erring toward charging on a missing/ambiguous postcode); the quoted *total* still rides on the assistant's display string until structured pricing lands.
- **Photos deferred** (`TODO(slice5x/photos)`): `jobs` has no inline base64 and the Storage bucket is not stood up, so Postgres-mode admin rows show "no photo" — Mark still receives the photo by email.

**Verification.** `node --test` 111 (108 pass / 3 skip; the new pure + fake-client suites are `test/bookings-store.test.js`, `test/bookings-chat.test.js`, `test/bookings-admin.test.js`). Against **real local Supabase** (Docker, D-010): the guarded `[integration]` test runs `insertBooking → availabilityFromJobs → fetchBookingsFromJobs` and an overlap-rejected case, 16/16. pgTAP 18/18 (incl. the postcode-nullable check). **Live behaviour is NOT yet verified** — enablement is the sign-off step: set `BOOKINGS_STORE=postgres` in Netlify, redeploy, then on a deploy preview confirm a real booking lands in `jobs`, availability reflects it, the admin merges it with legacy Blobs, the 09:00–16:00 grid is active, and a forced overlap returns 409. Supersedes the standalone Slice 5c.

**Addendum (14 June 2026) — ENABLED in production.** Done the same session as the merge, on Ben's go-ahead (the company is pre-launch — not operating, no marketing — so real booking risk is slim and the fail-closed design + flag rollback make it low-stakes). Steps taken: (1) the four Blobs bookings were confirmed to be **past** test data via the admin API (no real future bookings to diverge); (2) the hosted write path was verified by running the booking `[integration]` test against **hosted** Supabase (insert → availability → read-back → overlap-rejected → cleanup, 16/16, no emails — the 5a-style verification); (3) `BOOKINGS_STORE=postgres` was set in Netlify (POST `/env`, no `scopes`, L-018) and a production build triggered; (4) the deployed `check_availability` was confirmed to return the **9..15 Postgres grid** (`["9:00".."15:00"]`), proving the live function switched stores. The postcode-nullable migration was applied to hosted via the **Supabase Management API** rather than `db push` (no DB password in `.env`; see **L-019**) and catalog-verified (`is_nullable=YES`, both invariants intact). A full live `confirm_booking` was deliberately **not** run, to avoid an operator email to Mark; the hosted write is covered by the IT, and the first real booking is the true end-to-end (fail-closed if anything is off). **Rollback = unset `BOOKINGS_STORE` + redeploy.**

## D-022 — Admin auth via Supabase Auth (Slice 5d): per-user login, allowlist, publishable-key verification
**Status:** Accepted + LIVE (Slice 5d, June 2026; merged PR #44 `31225e1`; operator login verified by Ben 15 June)

The admin dashboard moves off the shared `ADMIN_SECRET` Bearer onto **per-user Supabase Auth** (email + password). This is the shared identity model the field app reuses (D-012), and it removes the single shared secret that gated all of Mark's customer data.

- **Sign-in is the Supabase Auth password grant from the browser.** `admin.html` POSTs email+password to `{SUPABASE_URL}/auth/v1/token?grant_type=password` with the publishable key as `apikey`, and uses the returned `access_token` as the `Authorization: Bearer` on every `/api/*` call. The project URL + publishable key are **public by design** and embedded in the page (RLS + the allowlist are the controls, not key secrecy).
- **The server verifies the JWT, then checks an allowlist.** New `server/netlify/functions/adminAuth.js` `requireAdmin(event)` validates the Bearer token with a publishable-key client (`auth.getUser`) and requires the user's email to be in `ADMIN_EMAILS` (default: the two operators). `bookings.js` and `handoffs.js` call it in place of `safeEqual(ADMIN_SECRET)`. **Two layers on purpose:** public sign-ups are **disabled** in the project (so only the dashboard-created operators exist), and the allowlist is defence-in-depth so a stray/accidentally-created user still cannot read the dashboard. Fails closed: no token → 401, unconfigured → 503, invalid → 401, not-allowlisted → 403.
- **Hard swap, not dual-auth.** `ADMIN_SECRET` is no longer read by any function (kept as legacy in `.env.example`, removable from Netlify once 5d is confirmed live). The cutover is verified on a deploy preview before merge; rollback is a revert. A dual-auth transition (accept either the JWT or the old secret) was considered and rejected as keeping the very secret 5d removes; pre-launch with trivial rollback, the clean swap is safe. `safeEqual` is retained as a tested constant-time utility.
- **Why publishable-key verification, not the service-role client.** Verifying a *user* token with the publishable (anon) key is the standard pattern and keeps the service-role key strictly on the data path (`supabaseClient.js`). The publishable key has a code fallback so there is no new required env var; `SUPABASE_PUBLISHABLE_KEY` overrides it for rotation.

**Verification.** `node --test` 134 pass / 3 skip (`test/admin-auth.test.js` pins no-token/invalid/not-allowlisted/allowlisted/unconfigured; the handoffs handler auth tests updated). **NOT live-verified:** before merge, on a deploy preview — sign in as each operator (loads bookings + handoffs), a wrong password is rejected, and a token for a non-allowlisted user gets 403. Full per-user RLS (functions acting AS the user, not the service role) is a later step, not needed for a two-operator admin. **Depends on 5e-2** (stacked branch): merge 5e-2 first or rebase.

## D-023 — Handoff-lead retention and erasure (review finding A5): manual erasure now, timed purge proposed
**Status:** Accepted (erasure mechanism built; full erasure is a documented two-part procedure); retention period proposed, pending Mark + DP confirmation

Handoff-lead rows (`kind='human_handoff'`, `customer_id` null) hold customer PII (contact, question, conversation snippet, any draft reply) with, until now, no erasure path and no retention limit. This separates the two UK GDPR duties:

- **Right to erasure (Art.17): built.** A manual, admin-triggered hard delete (`action:"erase"` in `handoffs.js` via `deleteHandoff`), hard-guarded to `kind='human_handoff'` so it can only ever remove a handoff, never a booking or other message, and allowed in **any** status (including a sent handoff, because an erasure request can arrive after the reply). `admin.html` shows an "Erase lead" button on each handoff card behind a confirm. `messages` is a leaf for handoffs (nothing references it), so the delete leaves no orphans. This is both the Art.17 tool Mark needs today and the primitive a timed purge reuses.
- **Storage limitation (Art.5(1)(e)): proposed, not yet built.** Default proposal: an unconverted handoff lead is retained a maximum of **6 months** from `created_at`, then purged. The period is a Mark/DP decision to confirm at the data-protection review; the scheduled purge (Supabase `pg_cron`, or a scheduled Netlify function deleting `human_handoff` rows older than the period) is the next step, deliberately deferred until the period is confirmed and the scheduling mechanism chosen, so an unagreed retention period is not baked in. `TODO(D-008/retention)` in `shared/messages.js` is repointed at this record.

**Why hard delete, not redact.** A lead has no business linkage (no job or invoice), so storage limitation favours deleting the row over keeping a redacted stub. The function log keeps the id plus an "erased" line for an audit trail, never PII.

**Erasure is a two-part operational procedure, not just the button (property-reg review).** The button deletes the database row only. The same lead PII was also emailed to Mark at escalation (`sendEscalationEmail` in `chat.js`), and any approved reply was emailed to the customer. Art.17 erasure is not complete until Mark also deletes the matching escalation email from his inbox; a reply already sent to the customer sits in the customer's mailbox, outside ICC's control, and cannot be recalled. Before erasing on request, Mark should confirm the request came from the contact held on the lead (Art.12(6) identity check). An automated mailbox-deletion integration would be disproportionate for a two-operator business, so these stay manual steps in the written erasure procedure; the dashboard button is one part of it. A durable PII-free erasure register (timestamp + id, for Art.5(2) accountability) is a noted future enhancement over the current ephemeral function-log line.

**Relationship to D-008.** D-008 governs PECR consent for marketing to past customers; this record governs retention and erasure of pre-conversion leads. A lead that converts to a booking follows the customer/job retention schedule, not this one.

**Verification.** `node --test` (the `handlePost erase` cases: success with `status:"erased"`, kind-guard 404 with nothing deleted, allowed on a sent handoff). Independent property-reg (UK GDPR) review of the change.

**Addendum (18 June 2026) — the stated retention periods.** The public privacy notice now discloses the periods this record governs: **around 6 years** for booking and financial records (HMRC / accounting), and **no longer than 6 months** for enquiries that do not convert (the lead-retention proposal above). Both remain subject to the data-protection professional review before go-live. This addendum records only that the periods are now disclosed to customers; the automated Art.5(1)(e) **timed purge is still unbuilt** (`TODO(D-008/retention)`).

**Addendum (19 June 2026) — the timed purge is now built; mechanism = a daily Netlify scheduled function.** The open decision in this record ("the scheduling mechanism chosen") is resolved: a **Netlify scheduled function** (`server/netlify/functions/purge-handoffs.js`, `schedule = "@daily"` in `netlify.toml`), chosen over Supabase `pg_cron` so the purge is unit-tested under `node --test`, observable beside the other functions, and parameterised from one config file. The retention period lives in `shared/config/retention.js` (`HANDOFF_LEAD_RETENTION_MONTHS = 6`, the figure the privacy notice states; the D-006 single-source rule, so the promised period and the enforced period cannot drift). The purge deletes `messages` rows with `kind='human_handoff'` and `created_at` strictly before the cutoff — **hard-guarded to the handoff kind** (it can never touch a booking or other message) and **bounded to already-expired rows** (idempotent and safe to trigger at any time, so the endpoint needs no auth gate). `getSupabaseAdmin()` null is a logged no-op (fail-safe: it does not purge until configured; `SUPABASE_*` are already set in prod). The cutoff is computed in **UTC** (`setUTCMonth`), not local time, because local-time month maths shifts by an hour across a DST boundary (the L-005 rule). Erasure on request (Art.17) is unchanged, including its manual second part (Mark deleting the escalation-email copy). The retention period stays subject to the DP review — if it moves, it is a one-line change in `shared/config/retention.js` (shortening is always safe; lengthening past the notice's "6 months" requires updating the notice in the same change, pinned by a test). **Verification:** `node --test` (cutoff maths incl. two DST/year-rollover cases; the kind-guard + cutoff filter via a fake client; the publicly-committed 6-month figure pinned). The schedule's live registration is confirmed on the Netlify deploy (Functions tab / deploy log).

## D-024 — Not VAT-registered initially (prices flat, no VAT); accounting integrates with FreeAgent
**Status:** Accepted (19 June 2026). **Accounting/invoicing half SUPERSEDED by [D-026](#d-026--invoicing-owned-by-the-platform-on-stripe-the-freeagent-as-accounting-hub-half-of-d-024-is-superseded) (18 August 2026)** — invoicing is platform-owned on Stripe, not FreeAgent. The VAT half (not registered; flat pricing) stands.

**VAT.** Mark trades as a sole trader and is **not VAT-registered** initially (he expects to stay under the ~£90k threshold for now; he can register later if turnover requires it). So ICC does not charge or display VAT: every price is a flat figure, the figure the customer pays. This supersedes the earlier "+ VAT" assumption that had been encoded across the site, the assistant prompt, the `/api/v1/quote` endpoint and the booking email/PDF (all quoted "£X + VAT"); all were swept to flat pricing on `fix/remove-vat-not-registered`. The listed numbers are unchanged — £75 stays £75 (Ben's call, confirmed 19 June) — they are simply no longer "+ VAT". `shared/config/pricing.js` is the single source: flat `price` figures and a VAT-free `quote()` (items total + flat £15 out-of-area surcharge + 10% deposit on the total, no VAT line). If Mark registers later, reintroduce VAT there and the site/prompt/quote follow. Some internal DB columns keep legacy `_ex_vat` names (renaming needs a migration); with no VAT the net equals the gross, so the stored values stay correct.

**Accounting = integrate with FreeAgent, don't rebuild it.** Mark's accounting software is **FreeAgent**. This resolves the deferred build-vs-integrate question (raised when scoping the operational backend) toward **integrate**: FreeAgent already does, and is HMRC-recognised for, the regulated/commodity half — invoicing, automatic payment chasing, bank feed + reconciliation (open banking), MTD filing, and built-in accountant access ("invite your accountant"). Rebuilding any of that for a sole trader would be disproportionate, regulated and worse than the tool. So the ICC platform is the **operational system of record** (jobs → quotes → invoices) that **feeds** FreeAgent via its REST API / OAuth2; FreeAgent owns the ledger and the filings. The platform builds only what FreeAgent cannot: marketing churn / re-engagement (Phase 4, consent-gated, D-008), review requests, the operational diary/CRM, and the AI booking assistant. This is the D-004 philosophy (let the specialist own the regulated layer) applied to accounting. The FreeAgent integration is gated on Mark setting up API access (an OAuth app + authorising), built behind credentials like the Supabase wiring. FreeAgent is free with some bank accounts (NatWest/RBS/Mettle) and paid otherwise (cf. Mark's likely Revolut, D-004).

**Relationship to D-011.** D-011's out-of-area surcharge stands; it is now a flat £15 (no "+ VAT"). The earlier "£15 + VAT" wording in D-011 and elsewhere is superseded by this record.

**Addendum (19 June 2026) — FreeAgent invoice flow = review-then-send.** Ben raised that invoices must reach clients quickly and questioned whether FreeAgent should own them. Checking the FreeAgent API resolved it: the integration is the *fast* path, not the slow one. Confirmed programmatic support — `POST /v2/invoices` (lands as Draft), `POST /v2/invoices/:id/send_email` (emails the client, custom subject/body or a template), `GET /v2/invoices/:id/pdf`, plus `payment_terms_in_days`/`due_on` and a `payment_url` (Stripe/GoCardless/PayPal). **Chosen flow:** on job completion the backend creates a **draft** invoice in FreeAgent (find-or-create contact by email, line items from the job, due date, pay link); the admin surfaces it; Mark **reviews the final amount** (often confirmed on the day, so a human check beats fully-automatic) and **one-click sends**; FreeAgent emails the client and the status flows back to our dashboard. FreeAgent then matches the payment (bank feed) and chases overdue invoices itself. So Mark never logs into FreeAgent to send — the platform drives it, in seconds. **Rejected:** the platform generating + sending its own branded invoice and syncing to FreeAgent — it puts invoices in two systems (numbering + drift + double maintenance), the "rebuild" cost this decision avoids; the only thing it buys is full brand control of the invoice email, which is not worth it. **Build notes** (next slice, behind a credentials flag): a `freeagent_invoice_id`/status pointer on the job; an OAuth2 client (refresh token server-side); draft-on-complete + one-click-send + status-reflect (poll or deep-link; webhook availability unverified). Gated on Mark creating a FreeAgent API app (Settings → API) + a one-time authorise.

## D-025 — Post-job review requests over email + SMS; SMS pulled forward; The SMS Works
**Status:** Accepted (15 July 2026) — built behind env flags, dormant until Mark's Google review link + SMS account exist

The DESIGN §9 review engine ("when Mark marks a job complete, the customer receives a request with a direct link to leave a Google review") is built, extended to **SMS as well as email** at Mark's request. This pulls the SMS channel forward from its "Phase 5, optional" slot (ROADMAP) — the recorded reason CLAUDE.md's working discipline asks for before bringing future-phase work forward: Mark specifically wants review-request texts, and reviews are a DESIGN "build early" priority, so the two land together.

**Trigger = one-click "mark complete & request review" (human-gated).** A button on each admin booking card sets `jobs.status='completed'` and immediately sends the review ask over both channels (`server/netlify/functions/reviewRequest.js`, `POST /api/review-request`). Chosen over an automatic fire-on-status-change (nothing else sets `completed`, and a human confirming the job is done is the natural gate) and over a review-then-send draft gate (the message is a fixed template, not per-customer AI text, so the handoff-style approval step buys nothing). Same admin gate as the other admin functions (`requireAdmin`, Supabase Auth).

**Provider = The SMS Works, behind a provider-agnostic adapter.** Researched UK gateways (15 July): The SMS Works ~3.1p + VAT per delivered text, GBP billing, credits never expire, pay-only-for-delivered, 50 free test credits; Twilio ~4.4p ex VAT, USD, but one account could also do WhatsApp/two-way later; BulkSMS/ClickSend similar to SMS Works but with worse terms (expiring credits / segment billing). At ICC's volume (tens of texts/month, ~£1–4/month) cost is a rounding error, so the call was billing-simplicity + best-fit for a low-volume UK sole trader → **The SMS Works**. The gateway is isolated in `smsProvider.js` behind `sendSms()`, so switching to Twilio is a new ~20-line adapter, not a rewrite (`SMS_PROVIDER` selects the adapter; default `smsworks`). Sender is an alphanumeric ID (`SMS_SENDER_ID`, default `ICCleaning`, 4–11 chars) — one-way, so no STOP handling to build; a review ask is a single transactional message, not a campaign.

**No schema change — the model already anticipated this.** The init migration's `messages` table already carries a `review_request` kind, a `channel` column, the draft→sent lifecycle and a comment marking `review_request` **transactional (no consent gate)** — an existing customer, the service just delivered, so it is not PECR direct marketing (contrast D-008 reengagement, which *is* consent-gated). Each send logs one `messages` row per channel: `status='sent'` **only after the provider accepts** (fail-closed, mirroring the handoff send), `status='failed'` otherwise, and a failure on one channel never blocks the other. **Idempotent per channel:** a channel already `sent` is *always* skipped, so the admin "Retry" button (shown once any channel has sent) only sends the channels that have not gone yet — recovering a failed SMS can never double-send the email that already succeeded, and a repeat click never double-messages the customer. (A pre-merge adversarial review caught the earlier `resend:true`-overrides-all design, which double-emailed on partial-failure recovery; per-channel skip replaced it.) The admin annotates each booking with `review_sent` (a `messages` lookup in `bookings.js`) so the button becomes a "✓ Review requested" note once done.

**Dormant until configured** (the house env-flag pattern, as with Supabase/FreeAgent): with `GOOGLE_REVIEW_URL` unset the button still marks the job complete but sends nothing (reported back per channel); with `SMSWORKS_API_KEY` unset the SMS channel reports "not configured" and only the email goes. Enabling is three Netlify env vars, no deploy.

**Gated on Mark (out-of-repo):** the Google review link comes from his Google Business Profile (still being set up per the launch docs — the "Ask for reviews" panel gives a `g.page/r/...` short link, which also keeps the SMS to one segment); and a The SMS Works account he owns (D-009) for the API key. Customer-email identity for the review email reuses the A4 controller-identity work (sign-off + privacy link + `CUSTOMER_REPLY_TO`).

**Verification.** `node --test` — the pure builders (`review-messages`), the number normalisation + adapter + fail-closed (`sms-provider`, injected fetch), and the endpoint's routing/gates/idempotency/fail-closed logging (`review-request`, fake Supabase + injected send fns); 193 total, 190 pass, 3 pre-existing skips. The admin card rendering was DOM-verified in the browser (button in the booked state, "✓ Review requested" + Resend in the sent state, no button on legacy Blobs rows).

## D-026 — Invoicing owned by the platform on Stripe; the FreeAgent-as-accounting-hub half of D-024 is superseded
**Status:** Accepted (18 August 2026) — supersedes the accounting/invoicing half of D-024 (its VAT half stands)

**What changed and why.** D-024 resolved accounting toward "integrate with FreeAgent, don't rebuild", with FreeAgent owning invoicing (create draft → Mark reviews → one-click send). Two things surfaced since that make FreeAgent the wrong hub for ICC:

1. **Mark is souring on FreeAgent generally** (from his Regency Cleaning use): some invoice emails do not reach certain recipients, and bulk client management is awkward. Building ICC's invoicing deeper on a tool he wants to move away from is the wrong direction.
2. **A second business needs a second FreeAgent account and its own subscription** (confirmed Aug 2026 against FreeAgent's own support + pricing pages): ~£19/mo ex VAT sole-trader, or free only if *that* business has its own eligible NatWest/RBS/Ulster/Mettle bank account (10% self-referral discount otherwise). Mark cannot add ICC under his Regency account. So "integrate" costs a second accounting subscription for a tool he dislikes — failing both "same or less money" and "painless".

**The reframe.** D-024 conflated two separable layers:
- **Invoicing** (create → send → get paid → track) — *the platform owns this*, driven entirely from our admin. This is what Mark asked for ("do it all through the backend").
- **Accounting / MTD** (digital records, quarterly ITSA, bank reconciliation, filing) — *Mark's / his accountant's duty on his whole tax position, not per-business*. The platform feeds it; it does not marry it.

Our invoicing does not conflict with MTD: MTD for Income Tax is an obligation on Mark as an individual, handled once across his combined income, not something an invoicing tool must "own". See the MTD note below and [docs/MTD_AND_ACCOUNTING.md](docs/MTD_AND_ACCOUNTING.md).

**Decision.**
- **The platform is the invoice system of record.** The existing `invoices` table (init migration `20260605115456_init.sql`: `id, job_id, status[draft|sent|paid|overdue], amount_ex_vat, issued_at, due_at, paid_at`) is the local record; `messages.invoice_id` and the `invoice_chase` message kind already anticipate our owning invoice comms.
- **Stripe is the invoice + payment rail (Stripe Invoicing), driven from our backend.** No monthly SaaS fee; cost is standard Stripe processing (~1.5% + 20p per *card* payment, verified Aug 2026), and nothing when a customer pays cash/bank and Mark marks it paid. This is also the payment rail D-004 already defaults to, so invoicing and deposits/balances share one vendor instead of two.
- **A provider adapter isolates Stripe** (mirroring `smsProvider.js`), so D-004's still-open Stripe-vs-Revolut processor choice, or a later move to our-own-PDF + a payment link, is a contained swap, not a rebuild. We keep our own invoice numbering/record so we are never locked out of our own history.
- **Accounting/MTD stays thin and swappable:** a data export/feed (CSV/JSON first; an API push to whatever tool Mark lands on, later), not a hard dependency. Regency can stay on FreeAgent; ICC need not.
- **Dormant behind a credentials flag** (the house pattern, D-025 / Supabase): with `STRIPE_SECRET_KEY` unset the invoice actions report "not configured" and change nothing.

**What was verified (Aug 2026, primary sources).**
- FreeAgent's API *is* capable of the hybrid we considered (`GET /v2/invoices/:id/pdf`, a `payment_url` field, and `PUT /v2/invoices/:id/transitions/mark_as_sent`, which marks an invoice sent *without* FreeAgent emailing it). So this decision is **not** driven by an API limitation — it is driven by the cost, lock-in and deliverability reasons above.
- Stripe UK: no monthly/setup fee; UK cards 1.5% + 20p; standalone Invoicing adds a small per-paid-invoice fee (~0.4%, capped) on top of processing — confirm the exact tier at build.
- Alternatives weighed: Zoho Invoice (free, but the free plan reportedly caps customers at ~5 — a dealbreaker for a growing book), Invoice Ninja (open-source, self-host ~£5–12/mo hosting + another system to run), Xero / QuickBooks (~£15/mo+, same per-business-subscription shape as FreeAgent). Stripe wins on no monthly cost and being the rail we already commit to.

**Relationship to other records.**
- **D-024:** its VAT half stands (not VAT-registered; flat pricing). Its accounting/invoicing half — "integrate with FreeAgent" and the review-then-send-via-FreeAgent addendum — is **superseded by this record**. The review-then-send *flow* (draft → Mark reviews the amount → one-click send) is kept; only the engine changes from FreeAgent to Stripe.
- **D-004:** unchanged. Stripe remains the default processor; Revolut Merchant remains the alternative. The adapter boundary here keeps that choice open.
- **D-002 / D-003 / D-009:** Supabase holds the invoice record; the API stays the single client-agnostic surface (website + field app); Mark owns the Stripe account from the start.

**MTD note (urgent — flagged for Mark's accountant, not settled here).** Whether Mark must keep MTD-digital records now depends on his *combined* Regency + ICC + any property gross income against HMRC's Making Tax Digital for Income Tax thresholds. Per web sources (ICAEW etc., **not** the in-force legislation in front of us — confirm on GOV.UK / legislation.gov.uk and with his accountant; these dates have moved before): mandation from 6 April 2026 for combined gross income over £50k, 6 April 2027 over £30k, 6 April 2028 over £20k. Because Mark is one individual running two trades, ICC being new does not by itself keep him out. This does not change the invoicing build (that layer is separable) but it sets whether he needs an MTD accounting tool at all, and when. Plain-English brief + action list in [docs/MTD_AND_ACCOUNTING.md](docs/MTD_AND_ACCOUNTING.md). Not tax advice.

**Build notes (the next slice — dormant behind `STRIPE_SECRET_KEY`).**
1. **Schema (small migration):** extend the existing `invoices` table with a provider pointer — `provider` (default `stripe`), `provider_invoice_id`, `number`, `payment_url` — and extend the `invoice_status` enum to cover Stripe states not present today (`void`, `uncollectible`) or map them to the existing four. Ship a post-apply catalog verification query (db-migration discipline).
2. **Adapter:** `server/netlify/functions/invoiceProvider.js` — `createDraftInvoice()`, `sendInvoice()`, `getInvoiceStatus()`; Stripe adapter first, `INVOICE_PROVIDER` selects it (default `stripe`). Keys server-side only.
3. **Endpoint (admin-gated):** on job completion (or a distinct "create invoice" action), find-or-create the Stripe customer by email, build line items from `shared/config/pricing.js` + the job, create a **draft**. Idempotent (skip if `provider_invoice_id` already set); fail-closed.
4. **Admin UI:** surface the draft (line items + amount), let Mark **review/adjust the amount**, then one-click **Send** (Stripe finalize + its hosted page, and/or we email via Resend with the hosted `payment_url` for deliverability). Show reflected status.
5. **Webhook:** `server/netlify/functions/stripe-webhook.js` — verify the Stripe signature (the SDK's constant-time verify), map `invoice.finalized|paid|payment_failed|voided` → our `invoice_status`. The status-reflect / verify-the-effect piece.
6. **Accounting feed:** a minimal invoices+payments export for Mark's accountant/MTD tool; keep it generic (no tool-specific coupling yet).
7. **Tests (real paths, injected fetch / fake Stripe, as `sms-provider` / `review-request` do):** adapter routing, find-or-create, pricing→line-items, idempotency, fail-closed, webhook signature + status mapping; `admin-html-syntax` if admin.html changes.
8. **Gated on Mark (out-of-repo, D-009):** a Stripe account he owns + API keys + the webhook endpoint registered; and the D-004 processor confirmation (Stripe vs Revolut). Switch-on = set env vars + redeploy (Netlify picks up env only on the next deploy, L-018).

## D-027 — Trading hours: per-day start windows with a 1pm last job start; Saturday premium
**Status:** Accepted (Mark, 24 August 2026). Supersedes the single 09:00–16:30 all-days model in `shared/config/tradingHours.js`.

Mark set the working day at the 24 Aug 2026 meeting. The day is now **day-specific** and shorter, with **1pm the last time a job may START** (Ben confirmed 1pm is the last start, not the hard close; the finish follows from the job's length). Start windows: Monday 09:30, Tuesday 10:30, Wednesday 09:30, Thursday 10:00, Friday 09:30, Saturday 09:30; Sunday closed. Saturdays carry a **weekend premium** (figure still to set).

This replaces the single-set-of-hours-for-all-days model (the D-006-pattern `tradingHours.js`, which encoded one 09:00–16:30 day with a 15:00 last start). Consequence: `tradingHours.js` becomes **per-day**, and the change ripples through everything that reads it: the assistant prompt (`chat.js`), the availability grid, `validateBooking`, `contact.astro` and the LocalBusiness JSON-LD (the F2 single-source lesson, that whatever the booking engine accepts is what the site and assistant may offer). Still open: the Saturday premium figure and rule (percentage or flat), and whether the public-facing GBP/JSON-LD "close" shows the 1pm last-start plus a typical job length. Recorded from the meeting minutes (`deliverables/ICC-Meeting-Minutes-Mark-Aug2026.md`, D6 and section 4.1).

**Addendum (2026-09-01) — provisional-booking behaviour, hardened by a cross-agent review.** The two "still open" items above are now decided, and the soft close gained a safety mechanism. The design was put through a cross-agent review (Claude as hub, GPT and Gemini as independent seats; converged record at `C:\Users\bengr\agent-exchange\REVIEW_d027-3pm-provisional_2026-09-01.md`); the build plan with a checkpoint log is `docs/D027_PROVISIONAL_PLAN.md`.
- **Auto-confirm line, 15:00, finish-based.** A booking whose FINISH (start plus slots times 60 minutes) is at or before 15:00 auto-confirms as before; a later finish is taken and HOLDS the slot but is held provisional for Mark (Ben confirmed 2026-09-01 it is finish-based, so a 1pm one or two hour job auto-confirms; only a finish after 15:00 needs Mark). `auto_confirm_by` is its own single-source constant in `tradingHours.js`, kept SEPARATE from the JSON-LD close (a coherence test binds them) because they are different facts (an approval policy vs a marketing schedule); coupling them would let a schema change silently move booking authority.
- **Advertised (JSON-LD) close, 3pm.** Ben chose "the site to say until 3", so `jsonld_close_job_hours` is 2 (1pm plus 2h). It coincides with the auto-confirm line, so a job finishing past the advertised close is exactly the one that needs Mark; no split-brain. This closes the "whether the public close shows the 1pm last start plus a typical job" item above.
- **Provisional model is hold, not escalate (Option A).** The late-finish job is a held booking (`status='booked'`, so the existing exclusion constraint holds the slot) plus a new `confirmation_state='awaiting_operator'`, not an escalation/lead. Option B (route as an enquiry, do not hold) was weighed and rejected: Ben wants the slot committed on the customer's click, matching Mark's willingness to work afternoons.
- **Mark accepts or declines from a one-click EMAIL link, not the admin.** Ben's steer, keep it simple for Mark. The operator email carries one neutral link to a read-only confirm page with the token in the URL FRAGMENT (a mail-scanner prefetch cannot act; the page's buttons POST). The admin keeps a fallback control because the email can fail.
- **Token is a stored SHA-256 hash, single-use, expiring** (end of the booking day), not a stateless HMAC (GPT's stance, adopted): revocable, and safe against both a signing-secret leak and a DB leak. Constant-time comparison, reusing the `safeEqual` pattern at `server/netlify/functions/bookings.js:8`. Idempotency is an atomic compare-and-set on `confirmation_state`.
- **Schema:** migration `20260901200000_jobs_confirmation_state.sql` (`confirmation_state` enum plus column, `operator_decided_at`, three `operator_action_token_*` columns, two `message_kind` values), applied and catalog-verified 2026-09-01.
- **Build status:** Phases 1 to 3 (availability minute-fix, the 15:00 / 3pm config, the write path, the operator and customer surfaces) are committed on `feat/d027-per-day-hours`; the accept endpoint and confirm page, the admin controls, and final verification (Phases 4 to 6) remain. The Saturday premium figure is still unset (unchanged).

**Addendum (2026-09-02) — Phases 4 and 5 built (public accept/decline + admin fallback), hardened by a second cross-agent review.** Phase 4 (the public token endpoint + confirm page) and Phase 5 (the admin fallback) are built on `feat/d027-per-day-hours` (not yet merged; Phase 6 gates it). The Phase 5 design went through a second cross-agent review (Claude hub + GPT + Gemini; converged record `agent-exchange/REVIEW_phase5-admin-fallback_2026-09-02.md`), which expanded and hardened it.
- **Public path (`/api/booking-action`, `bookingAction.js`).** POST-only; authorised SOLELY by the single-use, SHA-256-hashed, expiring action token (constant-time compare); the plaintext lives only in Mark's email link. Actions `view`/`accept`/`decline`; the token-expiry gate lives here, as a token concern.
- **Admin fallback (`/api/booking-admin`, `bookingAdmin.js`).** Authorised SOLELY by `requireAdmin` (never a token), for when the email link is lost or spam-filtered. `accept`/`decline` reuse the shared CAS with NO expiry gate, so a stale awaiting booking whose token has expired is still resolvable from the admin (a public-endpoint 410 telling Mark to "use the admin dashboard" is absurd when he is on it — Gemini). `resend` rotates the token (mints a new one; the previous link dies) and re-emails Mark, refusing if email is unconfigured (else it kills the old link with no replacement) or the booking's day has passed. `retry_customer_notice` re-sends the CUSTOMER outcome for a RESOLVED booking whose notice never sent, derived from the FINAL state.
- **Shared core (`bookingDecision.js`).** One source for the decision compare-and-set, the token lifecycle and the customer notice, so the two auth paths cannot drift (extraction chosen over exporting a handler's internals — two real callers of security-sensitive code).
- **Hardenings adopted from the review:** the decision CAS BINDS to the verified token hash so a rotation revokes an in-flight old link (see L-025); the customer notice is ALWAYS logged (sent/failed) and made visible + retryable in the admin, closing the send-then-log gap (see L-026); resend + retry are single-winner (old-hash CAS) / capped + client button-disabled.
- **Still open (Ben's call):** a STRICT single-winner retry needs one migration (a `sending` `message_status` value + a partial unique `(job_id, kind)` index) plus upsert logging; the default ships read-then-send + button-disable + cap, accepting a low-harm duplicate of a non-contradictory email. Phase 6 (pgTAP, one real end-to-end ride, then merge) remains before any of this goes live.

**Addendum (2026-09-03) — the strict single-winner customer notice was built.** Ben chose to build the strict retry rather than ship the low-harm default. The provisional customer notice is now a strict single winner: at most one `messages` row per `(job_id, kind)`, claimed before the send.
- **Migration `20260903120000_messages_provisional_single_winner.sql`** (catalog-verified on the local stack, then applied to prod 2026-09-03): a `sending` value on `message_status` and a PARTIAL unique index `messages_provisional_notice_uniq` on `(job_id, kind) WHERE kind in ('provisional_confirmed','provisional_declined')`. Partial so `reviewRequest`'s email/sms rows, of which a job may have several, are untouched.
- **Claim-then-send (`bookingDecision.js`).** `notifyCustomerOutcome` now CLAIMS the row (moves it to `sending`) before sending, then settles it `sent`/`failed`. A lost claim (a `23505`, or an already-`sent`/fresh-`sending` row) returns without sending, so there is no duplicate email. Finding 5 (D-027 addendum 2026-09-02, L-026) is preserved: no email, no key, or a send failure still settles a visible, retryable row, now the same single row transitioned.
- **The claim is in JS, not `ON CONFLICT`**, because supabase-js upsert cannot target a partial index (L-028). It is a reclaim-UPDATE (a terminal `draft`/`failed` row, or a `sending` claim older than 15 minutes) then an INSERT; a `23505` means another sender holds the row.
- **The 15-minute stale-`sending` reclaim** frees a claim whose function died mid-send, so a dead sender can never permanently strand a notice. `booking-action`/`booking-admin` are synchronous functions (10s cap), well inside the window, so a live claim is never reclaimed.
- **Verification:** 40 pgTAP; default node suite 318 (310 pass); guarded integration against real Postgres proving the single winner for both the fresh-insert and the reclaim interleavings, plus the stale reclaim and retry-after-failed. Commits on `feat/d027-per-day-hours`: `667ed29`, `8efd081`, `7f09775`, `e4a1030`. **Reviewed, applied, merged and DEPLOYED (2026-09-03):** the cross-agent review converged (CLAUDE + GEMINI; `agent-exchange/REVIEW_d027-strict-retry_2026-09-03.md`), which added the Resend `Idempotency-Key` on `${kind}/${job.id}` and an enforcing concurrent-stale-reclaim test (`abeb2b5`); migration `20260903120000` was applied to prod and catalog-verified (`message_status` has `sending`, the index is present); the branch merged to `main` (`2f8a0ff`) and deployed (Netlify production `ready`). Remaining: the one real end-to-end ride as the first post-deploy check.

## D-028 — Travel charge re-centred on Mark's base address, tiered £5/£10/£15 (amends D-011)
**Status:** Accepted (Mark + Ben, 24 August 2026). Amends D-011 (flat £15 + three-town core).

At the 24 Aug 2026 meeting the travel-charge model changed. The free-travel area is now a radius from **Mark's base address** (base postcode GL3 3PT; the full street address is held privately in the meeting minutes per D-016, and is used only for the distance calculation, never shown to the customer), not the former Cheltenham/Gloucester/Winchcombe three-town core. Free within roughly **10 miles** of the base (covers Gloucester and, by distance, Cheltenham), then tiered surcharges by distance: **£5** near fringe (e.g. Winchcombe), **£10** (e.g. Cirencester), **£15** furthest afield (e.g. Moreton-in-Marsh, Bourton-on-the-Water, Stow-on-the-Wold). **Winchcombe moves from free (core under D-011) to about £5.**

This amends D-011: the flat £15 out-of-area surcharge and the three-town core are replaced by the base-centred tiered model. Consequence: `shared/config/serviceArea.js`, the area pages, the assistant quote, `/api/v1/quote`, and the GBP surcharge wording all change. Still open before coding: the exact distance bandings, the postcode-boundary approximation for each tier, and (Ben's steer) moving to a radius-from-base distance check rather than the current district/sector approximation. Reason for the re-centre: Intelligent is Gloucester-based (Mark's GL3 home), whereas Regency is Cheltenham, so centring free travel on the actual base is truer to cost. Server-side enforcement in `validateBooking` remains the D-011 open item and applies here too. Recorded from the meeting minutes (D7 and section 4.2).
